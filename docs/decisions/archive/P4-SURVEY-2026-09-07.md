# D115 P4 survey — files and functions over budget in `packages/web`, `packages/node`, `packages/cli`, `packages/core`

Repository: `/private/tmp/velar-d114/r0-baseline`, branch `refactor/r1-compiler-split`, HEAD `6742d73b` ("docs(decisions): D114 — the shared-runtime-validation split is done"). Read-only survey; nothing was modified and no gate was run.

> **File note:** my execution mode is strictly read-only (no Write/Edit tool, no shell redirection or heredocs), so `p4-survey.md` was not created on disk. The complete Markdown document follows below verbatim — save it to `/private/tmp/claude-501/-Users-mac-Documents-VelarScript/4eb05fb7-90d0-48ec-af5f-bf5a7df2a7a5/scratchpad/p4-survey.md` if you want it as a file.

---

## 0. Scope and reference shape

`file-budget-allowlist.json` (gate `scripts/check-file-budget.mjs`, decision D115, limits: file 800, function 120) holds **28 file entries** and **34 function entries**. Split by package:

| Package | allowlisted files | allowlisted functions |
|---|---|---|
| `packages/cli` | 6 | 19 |
| `packages/web` | 5 | 7 |
| `packages/node` | 2 | 1 |
| `packages/core` | **0** | **0** |
| `packages/compiler` (out of P4 scope) | 4 | 3 |
| `packages/create`, `packages/desktop` (out of scope) | 0 | 2 |
| `tests/` (P5) | 11 | 2 |

**`packages/core/src` has no file over 800 lines and no function over 120.** Its largest file is `index.ts` at 286 lines; the package was already split by R2b into `interfaces/<module>.ts` (18 files) plus `runtime/*.js` (22 files). It is not P4 work.

Allowlist entries are **ceilings, not current values** (`check-file-budget.mjs` §"The allowlist": red on (a) new violation, (b) growth past the recorded ceiling, (c) an entry that is now within limit or gone). Several in-scope entries have already dropped below their ceiling; both numbers are given throughout.

### The pattern being copied (D115 §三 + `docs/contributing/compiler-architecture.md` + D114 "R1f 落地")

`packages/compiler/src` has `lexer/ parser/ types/ analysis/ emit/ format/ semantic/` (19 directories including nesting, 333 source files). The composition-root ruling, from D114 line 509 onward:

> **裁决：`analyzer.ts` 是组合根，留在 3,251 行。** 余下内容是 166 个字段（协作者实时读取）、17 个宿主构造器（953 行）、66 条 protected 缝（551 行）、三个分派器与 25 个公开读取器。宿主构造器不能搬到别的模块——`private` 成员在类外不可读（TS2341，已在本仓 tsc 7.0.2 下实证）…D115 §五 P2 的完成条件「`analyzer.ts` ≤ 800 行」**修订为**「`analyzer.ts` 不含分析逻辑：只有状态、缝、宿主构造器与分派器」。

The architecture doc states the collaborator contract: *"A collaborator never names the `Analyzer` type; it declares the interface it needs (`AdvisoryHost`, `AnalyzerOwnedHints`, `CollectionInferenceHost`, `CallInferenceHost`, `MemberAccessHost`, and the twelve faces the expression directory declares), and that interface is the record of what it depends on. Analyzer state a collaborator reads mid-walk … arrives through getters on that interface, so the reads stay live."* The same shape is recorded for `lexer/` (eight collaborators, one live-reading host), `parser/` (`ClassParserHost`, `OperatorParserHost`, …; `parseStatementBody` a six-phase dispatcher), `emit/` (all 32 `protected` members still declared on `JavaScriptEmitter`, forwarding to the family that owns the body; the host built once and passed to all), `format/`, `semantic/`, `types/`.

D115 §三 names the intended target layouts for the P4 packages:

- `packages/web`: `analysis/{components,jsx,look,look-static,reactivity,resources-actions,lifecycle,watch-cycles}.ts`; `emit/{components,jsx,look,runtime-imports}.ts`
- `packages/node`: "`server-analyzer/parser/emitter` 保持但归入 `analysis/`、`emit/`"
- `packages/cli`: `project.ts` → `project/{graph,interfaces,incremental,entries,diagnostics}.ts`; `language-server.ts` → `lsp/{hover,completion,rename,semantic-tokens,diagnostics,transport}.ts`
- `packages/core`: `interfaces/<module>.ts` + `runtime/<module>.js` — **already landed** (R2b)

`check:module-map` (D115 §二, `docs/contributing/module-map.md`) **does not exist yet**: there is no `scripts/check-module-map.mjs` and no `docs/contributing/module-map.md`.

---

## 1. `packages/web/src/analyzer.ts`

**Lines now 5153 — allowlist ceiling 5154.** Largest file in the repository outside tests.

### 1.1 Top-level structure, in order

| Lines | What |
|---|---|
| 1–138 | imports (5 from `@velarscript/compiler`/`/extension`, 12 sibling modules incl. 5 from `./analysis/`) |
| 139–253 | tables + small helpers: `routeContextIdentity` (exported), `removedJsxControlAttributes`, `nativeDomEventNames`, `htmlEventHandlerAttributes`, `textualWebPrimitiveNames`, `webEventTypeNames`, `webEventDeadFields`, `diagnostic`, `bindTargetGuidance`, `LOOK_CONDITION_TERM_LIMIT`, 15 `look*` `ValueType` singletons, `lookPropertyType`, `LOOK_PROPERTY_TYPES`, `storageReadGuidance` |
| **254–436** | `export function inferWebIntrinsic` (**183 lines**) |
| 442–758 | Look value vocabulary: `lookSpacingFamily`…`literalStringValues` — builders, borders, durations, shorthand guidance, `LookFilterRewrite`/`CssFunctionValue` interfaces, `cssFunctionValues`, `topLevelWords`, `filterLength`, `filterNumericArgumentsHold`, `filterColor`, `filterFunctionRewrite`, `lookFilterRewrite`, `lookShorthandOverlapMessage`, keyword sets, `lookVocabularyLead`, `lookVocabularyGuidance`, `LookValueGuidance` |
| 769–803 | URL-attribute security: `WEB_URL_ATTRIBUTES`, `WEB_SCRIPT_URL_SCHEMES`, `WEB_INERT_DATA_MEDIA_TYPES`, `urlAttributeScheme`, `urlSchemeRefusal` |
| 804–885 | media/viewport conditions: `isViewportComparison`, `flippedComparisons`, `flippedViewportComparison`, `isSchemeCondition`, `mediaSubjectShape`, `lookMediaVocabulary`, `lookSourceOf`, `lookRuntimeSignature`, `lookKebab` |
| 886–1023 | Look condition terms & contributions: `lookOperatorNames`, `lookNegatedOperators`, `lookConditionAtom`, `lookConditionKey`, `lookConditionTerms`, `collectLookDeclarations`, `lookContributions` |
| 1024–1123 | Look sites/imports/builder names + CSS asset scanning: `LookValueSite`, `LookImportSite`, `collectLookImportSite`, `collectLookBuilderNames`, `lookConditionTermCount`, `firstRelativeCssAssetAddress`, `mentionsLookUnitType`, `lookLiteralZero`, `lookJoin`, `containsCssImport` |
| 1125–1204 | routes + JSX accessibility: `checkRouteComponent`, `checkRoutePath`, `containsPromise`, `hasAccessibleJsxContent`, `hasAccessibleSvgName` |
| 1205–1466 | retired accessors + watch-subject rendering: `RETIRED_ACCESSOR_INTRINSIC`, `retiredAccessorReaderType`, `RETIRED_ACCESSOR_TYPE`, `watchSubjectPath`, `WATCH_SUBJECT_TEXT_ESCAPES`, `renderWatchTextLiteral`, **`renderWatchSubject` (1297–1421, 125 lines)**, `renderWatchSubjectOperand`, `groupWatchSubject`, `RetiredAccessorDeclaration` |
| 1467–1800 | keyed-rebuild + JSX detection helpers: `keyedRebuiltRecord`, `FunctionDeclarationStatement`, `collectModuleFunctions`, `LoopBindingPattern`, `collectPatternNames`, `readsAnyName`, `buildsFreshRecords`, `WEB_REACTIVE_DECLARATION_LABELS`, `firstReactiveDeclaration`, `carriesWebNode`, `bodyReturnsJsx`, `subtreeHoldsJsx`, `returnedMarkupRoots`, `componentSpelling`, `rowFieldPassthrough`, `KeyedListRebuild` |
| **1801–5115** | `export class VelarWebAnalyzer extends Analyzer` (3315 lines) |
| 5117–5153 | `webTypeFields`, `webEventType` |

**Exports (3):** `routeContextIdentity`, `inferWebIntrinsic`, `VelarWebAnalyzer`.

### 1.2 The class: 43 private fields, 111 members (1 constructor, 1 public override, 15 protected seams, 94 private methods)

Fields (1802–1879, 78 lines): `componentStates`, `mountedDepth`, `cleanupDepth`, `watchBodyDepth`, `keyedListSources`, `keyedListRebuilds`, `moduleFunctions`, `synchronousReactiveDepth`, `jsxDepth`, `componentBodyDepth`, `resources`, `unsafeCssImports`, `probedOperandTypes`, `importedLookStaticValues`, `lookStatic`, `lookEntryScopes`, `derivedReactiveNames`, `checkedBuilderCalls`, `staticJsxKeys`, `honoredJsxKeys`, `reportedJsxKeys`, `lookBuilderNames`, `lookImport`, `lookDeclarations`, `lookLiteralDepth`, `computedBindingSpans`, `resourceBindingSpans`, `reactiveWriters`, `reactiveDerivations`, `reactiveStateNames`, `importedComputedNames`, `importedComputedSpans`, `retiredAccessorDeclarations`, `retiredAccessorReads`, `retiredComputedReferences`, `migratedComputedCallees`, `plainCallSpans`, `webModulePath`, `webSourceText`, `explicitReadonlyPropBindings`, `webPublicConfig`, `publicConfigNames`, `refusedBuilderCalls`.

**Methods grouped by the concern they serve** (lines = sum of member spans; fields named are the private state each group reads or writes):

| Group | Methods | Count | Lines | Private fields touched |
|---|---|---|---|---|
| **A — entry & program passes** | `constructor`, `analyze`, `reportBrowserTestImports`, `rejectWebOwnedTypeNames`, `reportStaticJsxKeys` | 5 | 141 | `webModulePath`, `webSourceText`, `resources`, `webPublicConfig`, `importedLookStaticValues`, `importedComputedNames`, `lookStatic`, `lookBuilderNames`, `publicConfigNames`, `lookImport`, `lookDeclarations`, `derivedReactiveNames`, `keyedListSources`, `keyedListRebuilds`, `moduleFunctions`, `reactiveWriters`, `reactiveDerivations`, `reactiveStateNames`, `staticJsxKeys`, `honoredJsxKeys`, `reportedJsxKeys` |
| **B — declarations / statement dispatch** | `predeclareExtensionStatement`, `analyzeExtensionStatement`, `analyzeStatement`, `prescanExtensionScopeDeclaration`, `markDeclaredBindingReactive`, `componentType`, `actionType`, `analyzeActionDeclaration`, `analyzeResourceDeclaration`, `analyzeComputedDeclaration` | 10 | 242 | `deferredExecutionDepth`, `synchronousReactiveDepth`, `unsafeCssImports`, `resources`, `computedBindingSpans`, `resourceBindingSpans`, `importedComputedNames`, `importedComputedSpans` |
| **C — components & ownership scope** | `analyzeComponent`, `containsReadonlyView`, `validateComponentHandleType`, `resolveAnnotation`, `normalizeComponentContracts`, `validateComponentHost`, `rejectUnownedComponentElement`, `rejectStatefulWebNodeFunction`, `ownershipScopeRejection`, `extensionOwnsFunctionlessReturn`, `invalidExtensionAwaitContext`, `invalidExtensionAwaitMessage` | 12 | 275 | `componentStates`, `componentBodyDepth`, `mountedDepth`, `cleanupDepth`, `watchBodyDepth`, `jsxDepth`, `synchronousReactiveDepth`, `explicitReadonlyPropBindings` |
| **D — reactive names / bindings** | `isComputedBinding`, `isResourceBinding`, `isImportedComputedBinding`, `writableStateName`, `calledComputedBinding`, `rejectComputedAssignment`, `directReadonlyPropMutation`, `rootBindingName`, `derivedReactiveRead` | 9 | 158 | `computedBindingSpans`, `resourceBindingSpans`, `importedComputedSpans`, `explicitReadonlyPropBindings`, `derivedReactiveNames` |
| **E — retired accessors (migration)** | `recordRetiredAccessorDeclaration`, `recordRetiredAccessorRead`, `reportRetiredComputedFunction` | 3 | 86 | `retiredAccessorDeclarations`, `retiredAccessorReads`, `retiredComputedReferences`, `migratedComputedCallees`, `plainCallSpans` |
| **F — watch / cycle / frozen reads** | `rejectFrozenWatchSubject`, `rejectWatchCycle`, `watchSubjectWrite`, `reactivePlaceType`, `zeroArgumentReader`, `frozenWatchSubject`, `frozenValueType` | 7 | 248 | `reactiveDerivations`, `reactiveStateNames`, `reactiveWriters`, `derivedReactiveNames` |
| **G — keyed list rebuild advisory** | `recordKeyedListRebuild`, `recordDerivedKeyedListRebuild`, `rebuildsRecordsPerElement`, `adviseKeyedListRebuilds` | 4 | 98 | `keyedListRebuilds`, `keyedListSources`, `moduleFunctions` |
| **H — Look entries / values / builders / tokens** | `analyzeLookEntries`, `reportLookSnapshotReads`, `checkLookBuilderCall`, `checkLookTokenCall`, `reportLookColorVarReference`, `lookTokenImportEdit`, `checkAnimateBuilderCall`, `checkAnimationKeyword`, `refusedBuilderCallWithin`, `reportLookShorthandOverlap`, `reportLookDirectiveOverlap`, `recordLookEntry`, `analyzeLookValue`, `isLookTokenCall`, `adviseLookFilterSpelling`, `localizeLookBuilderCall`, `lookBuilderRewrite`, `lookBuilderImportEdit`, `lookTokenCallText`, `lookTokenRewrite`, `adviseLookTokenSpelling`, `validateLookStringVocabulary`, `foldedLookKeyword`, `reportLookNumberWithoutUnit`, `inferLookCondition`, `checkViewportThreshold` | 26 | **765** | `lookStatic`, `lookBuilderNames`, `lookEntryScopes`, `checkedBuilderCalls`, `lookImport`, `lookDeclarations`, `refusedBuilderCalls`, `derivedReactiveNames` |
| **I — keyframes** | `analyzeKeyframes`, `reportKeyframeSnapshotReads` | 2 | 66 | `lookStatic`, `refusedBuilderCalls`, `derivedReactiveNames` |
| **J — JSX** | `inferJsx`, `reportIframeSrcdocSandbox`, `reportUrlAttributeScheme`, `checkKeyedInterpolation`, `reportIneffectiveJsxKeys`, `analyzeComponentElement`, `analyzeJsxLookAttribute`, `reportIndependentLookCollisions`, `analyzeComponentRef`, `analyzeNativeJsxAttribute`, `adviseNativeBooleanTextConversion`, `plainNativeTextAttribute`, `reportNativeAttributeSpelling`, `checkEventHandlerResult`, `isWritableBindTarget`, `bindPathRoot`, `analyzeBindGroup`, `eventAssignedStateBinding`, `analyzeInlineVisualAttribute` | 19 | **787** | `jsxDepth`, `staticJsxKeys`, `honoredJsxKeys`, `reportedJsxKeys`, `keyedListSources`, `lookDeclarations`, `webSourceText` |
| **K — type predicates** | `isLookInput`, `isClassInput`, `isJsxRenderable`, `isScalarTextType`, `isJsxAttributeValue`, `isOptionalString` | 6 | 99 | none (pure over `ValueType`, use base `expandAliases`) |
| **L — routes** | `checkWebRouteRecords`, `checkWebRouteComponent` | 2 | 44 | none |
| **M — expression inference plumbing** | `inferExtensionExpression`, `inferExpression`, `probedSlotType`, `extensionFieldsOf`, `inferExtensionCall`, `validateExtensionTypeSyntax` | 6 | 227 | `probedOperandTypes`, `plainCallSpans`, `lookBuilderNames`, `publicConfigNames`, `webPublicConfig`, `lookLiteralDepth`, `webSourceText` |

Groups H + J together are **1552 lines — 47 % of the class body**.

### 1.3 Called from outside the class

Every method is `private` except the constructor, `override analyze` and 15 `protected override` seams. Grepping `.methodName(` across `packages/web/src` (excluding `analyzer.ts`) and `tests/` returns **zero** hits for all of them. The seams are called only from the base `Analyzer` in `packages/compiler/src`:

| Member | callers in `packages/compiler/src` | in `packages/web/src` outside analyzer | in `tests/` |
|---|---|---|---|
| `analyze` (public) | 3 | 0 | 1 |
| `inferExpression` | 125 | 0 | 0 |
| `resolveAnnotation` | 39 | 0 | 0 |
| `invalidExtensionAwaitContext`, `invalidExtensionAwaitMessage` | 4 each | 0 | 0 |
| `analyzeStatement`, `markDeclaredBindingReactive` | 3 each | 0 | 0 |
| `prescanExtensionScopeDeclaration`, `inferExtensionCall`, `validateExtensionTypeSyntax`, `ownershipScopeRejection`, `extensionOwnsFunctionlessReturn` | 2 each | 0 | 0 |
| `predeclareExtensionStatement`, `analyzeExtensionStatement`, `inferExtensionExpression`, `extensionFieldsOf` | 1 each | 0 | 0 |

So the entire class except `analyze` and the 15 seams is internal. This is exactly the compiler's situation: host constructors would read `private` fields; moving them out of the class is TS2341.

### 1.4 Coupling

**Imports:** `@velarscript/compiler` (`mechanicalEdits`, `mechanicalFix`, `semanticTypeIdentity`, `Diagnostic`, `DiagnosticEdit`, `DiagnosticFix`, `Span`); `@velarscript/compiler/extension` (`Analyzer` + 25 names); `./browser-test.ts`, `./css-tokens.ts`, `./look.ts` (~40 names), `./look-static.ts`, `./keyframes.ts`, `./analysis/component-guidance.ts`, `./analysis/look-values.ts`, `./analysis/public-config.ts`, `./analysis/reactive-names.ts`, `./stable-order.ts`, `./emitter.ts` (`dynamicChildLeaves`, `JSX_SCALAR_TEXT_HINT`), `./types.ts`, `./elements.ts`, `./ast.ts`.

**Imported by:** exactly one source file — `packages/web/src/compiler.ts:3` `import { inferWebIntrinsic, routeContextIdentity, VelarWebAnalyzer } from "./analyzer.ts"` (used at `compiler.ts:986` `new VelarWebAnalyzer(context, extensions)`; `inferIntrinsic: inferWebIntrinsic`). No other package imports it. No import cycle: `emitter.ts` does not import `analyzer.ts`.

### 1.5 Runtime strings

**None.** No `String.raw` and no multi-line template literal. `packages/web/src` is in `rawTemplateScopes` of `scripts/check-runtime-boundary.mjs` (line 513–520), so a new one would be refused; the Web runtime is 40+ real `.js` files in `packages/web/runtime/`.

### 1.6 Functions over 120 lines

| Name | Lines now | Ceiling | Shape |
|---|---|---|---|
| `inferWebIntrinsic` (254–436) | 183 | 183 | **Single control-flow**: one line destructuring `context`, then `switch (intrinsic.name)` over the Web intrinsic roster to the end. Splits per intrinsic family (web/http/storage/forms/realtime/config), not by step extraction. |
| `renderWatchSubject` (1297–1421) | 125 | 125 | **Single control-flow**: `switch (expression.kind)` over expression kinds, each arm rendering the subject text. |
| `VelarWebAnalyzer.analyzeComponent` (3158–3279) | 122 | 122 | **Mixed**: a flat prologue (3159–3195: save/restore `constructorDepth`, `componentStates`, `explicitReadonlyPropBindings`, declare binding, one `for` over `statement.parameters`, handle-type validation, four counters) then one `for (const item of statement.body)` (3196–3271) that is a per-section dispatch (render / mounted / cleanup / expose / computed / resource / action / watch), then an epilogue at 3272. |
| `VelarWebAnalyzer.analyzeNativeJsxAttribute` (4532–4683) | 152 | 152 | **Flat sequence** of independent guarded steps: event-name derivation, bound-state check, `inferred` computation, then `if (attribute.name === "style")` … a long chain of independent `if` arms (style, look, class, bind group, event handler, spelling, attribute-value check) ending with two trailing advisories at 4680–4682. Splittable by extraction. |

### 1.7 Tests / gates that pin this file's internals

- `tests/web/api-contract.test.ts:310` reads `packages/web/src/analyzer.ts` **as text** and asserts `assert.match(webAnalyzer, /class VelarWebAnalyzer extends Analyzer/u)` and `assert.match(webAnalyzer, /function inferWebIntrinsic/u)`. Unlike the compiler layers in the same test, this is a **single-file read** — the test's own `compilerLayer(entry, directory)` helper (lines 294–301) reads an entry file *plus every `.ts` under a matching directory*; a Web split would need the same helper applied here or the assertions would still pass only if both names stay in `analyzer.ts`.
- `tests/web/any-guards.test.ts:9` names `packages/web/src/analyzer.ts` in a comment only (no import, no reach).
- No test imports a private-ish export from this file; no `as any` / `as unknown as` reaches into `VelarWebAnalyzer`.
- `scripts/check-runtime-boundary.mjs` does **not** read `packages/web/src/analyzer.ts`.

---

## 2. `packages/web/src/emitter.ts`

**Lines now 1365 — ceiling 1365.**

### 2.1 Top-level structure, in order

| Lines | What |
|---|---|
| 1–37 | imports: `@velarscript/compiler/extension`, `./ast.ts`, `./css-tokens.ts`, `./keyframes.ts`, `./look-static.ts`, `./look.ts`, `./runtime-sources.generated.ts` |
| 38–79 | `AssignmentStatement`, `LookStaticAtom`, `LookRuntimeAtom`, `LookConditionTerm`, `LookRule`, `DEFERRED_BODY_STATEMENT_KINDS` |
| **81–1071** | `export class WebJavaScriptEmitter extends JavaScriptEmitter` (991 lines) |
| 1072–1151 | JSX helpers: `normalizeJsxText`, `hasMeaningfulChildren`, `JsxKeyedList`, `DynamicChildGuard`, `DynamicChildLeaf` (exported interfaces), `jsxKeyedList`, `JSX_SCALAR_TEXT_HINT`, `containsJsxExpression`, `dynamicChildLeaves` |
| 1152–1330 | Look CSS lowering: `EMPTY_LOOK_TERM`, `LOOK_CONDITION_TERM_LIMIT`, `lookConditionTerms`, `LOOK_NEGATED_MEDIA_OPERATORS`, `viewportAtom`, `schemeAtom`, `combineLookTerms`, `lookToken`, `LOOK_RANK_SPAN`, `lookConditionDepth`, `lookVariable`, `lookDeclaration`, `lookOperatorName`, `lookMediaQuery`, `lookSelectors`, `lookHookSelectors`, `LOOK_TARGET_SELECTORS`, `kebab` |
| 1323–1365 | `visitLookExpressions`, `containsUnitLiteral`, `containsWebSyntax`, `webRuntime` |

**Exports (7):** `WebJavaScriptEmitter`, `JsxKeyedList`, `DynamicChildGuard`, `DynamicChildLeaf`, `jsxKeyedList`, `JSX_SCALAR_TEXT_HINT`, `dynamicChildLeaves`.

### 2.2 The class: 14 fields, 4 public, 12 protected seams, 22 private methods

Fields (82–110): `currentScope`, `moduleEvaluation`, `currentJsxNamespace`, `resourceContents`, `cssOutput`, `cssSegments`, `webOutput`, `needsFileTypeHelper`, `needsLookArithmeticRuntime`, `importedLookStaticValues`, `lookStaticValues`, `lookKeywordProperties`, `jsxId`, `keyframeNames`.

| Group | Members (start, length) | Lines | Fields read/written |
|---|---|---|---|
| **Entry & output accessors** (public) | `constructor` (111,15), `emit` (126,11), `css` (147,4), `styleSegments` (151,4), `web` (155,4), `outsideModuleEvaluation` (137,10) | 48 | `resourceContents`, `importedLookStaticValues`, `lookStaticValues`, `webOutput`, `needsFileTypeHelper`, `needsLookArithmeticRuntime`, `moduleEvaluation`, `cssOutput`, `cssSegments` |
| **Runtime-import selection** (seams) | `emitTypeCheck` (159,21), `emitIsCheck` (180,19), `additionalHelpers` (199,8), `webRuntimeHelpers` (207,15), `lookKeywordTable` (222,8), `reactiveBridgeHelpers` (230,11), `includesErrorNormalizationRuntime` (241,11), `detachedTaskHelpers` (252,38) | 131 | `needsFileTypeHelper`, `needsLookArithmeticRuntime`, `webOutput`, `lookKeywordProperties` |
| **Extension walking** (seams) | `visitExtensionRuntimeExpression` (290,21), `visitExtensionRuntimeStatement` (311,40), `extensionExpressionContainsDirectAwait` (351,7), `extensionStatementContainsDirectAwait` (358,8) | 76 | none |
| **Statement/expression dispatch** (seams) | `emitStatement` (366,50), `emitExpression` (416,79) | 129 | `moduleEvaluation`, `keyframeNames`, `lookStaticValues`, `currentScope`, `currentJsxNamespace`, `webOutput` |
| **Look emission** | `emitLook` (495,4), `emitLookEntries` (499,32), `emitLookValue` (531,4), `emitLookArithmetic` (535,4), **`prepareLooks` (940,130)** | 174 | `lookStaticValues`, `keyframeNames`, `lookKeywordProperties`, `resourceContents`, `cssSegments`, `cssOutput` |
| **Components** | `emitComponent` (539,107), `emitReactiveAssignment` (647,12) | 119 | `currentScope`, `currentJsxNamespace` |
| **JSX** | `emitJsx` (659,5), **`emitJsxCode` (664,137)**, `emitJsxChildren` (802,9), `emitJsxChildrenCode` (811,10), `emitDynamicChild` (821,35), `isScalarTextChild` (856,5), `emitDynamicChildLeaf` (861,16), `emitGuardedExpression` (877,18), `emitBindTarget` (895,9), `emitJsxAttributeValue` (904,6), `emitJsxLookValue` (910,17), `emitJsxStyleValue` (927,13) | 280 | `jsxId`, `currentScope`, `currentJsxNamespace`, `moduleEvaluation` |

### 2.3 Called from outside the class

- `css()`, `styleSegments()` — duck-typed public seams read by `packages/compiler/src/index.ts:344–345` (`emitter.css?.() ?? null`, `emitter.styleSegments?.() ?? null`); declared optional in `packages/compiler/src/extension.ts:146–147`.
- `web()` — public, no external caller found in `packages/`, `tests/`, `scripts/`.
- `emit()` — the base entry point.
- The 12 `protected override` members are base-class seams. The 22 private methods have no external caller.
- The class is constructed once: `packages/web/src/compiler.ts:1131` inside `createEmitter(...)`.

### 2.4 Coupling

**Imported by:** `packages/web/src/compiler.ts:13` (`WebJavaScriptEmitter`) and `packages/web/src/analyzer.ts:78` (`dynamicChildLeaves`, `JSX_SCALAR_TEXT_HINT`). Nothing outside `packages/web`.

### 2.5 Runtime strings

**None in TypeScript.** The runtime text arrives through `./runtime-sources.generated.ts` (135 lines, generated by `scripts/generate-runtime-sources.mjs` from `packages/web/runtime/*.js`). `webRuntime(foundation, lookKeywords)` at 1363 is a composer over generated constants.

### 2.6 Functions over 120 lines

| Name | Lines now | Ceiling | Shape |
|---|---|---|---|
| `WebJavaScriptEmitter.emitJsxCode` (664–800) | 137 | 137 | **Mixed**: an early `if (/^[A-Z]/u.test(expression.tag))` component branch (665–720, 56 lines — an independent, extractable step), then element setup (721–729), then two loops — `for (const attribute of expression.attributes)` (730–787, an attribute dispatch) and `for (const child of expression.children)` (788–798) — then one return. Two natural collaborators: component-element emission and native-attribute emission. |
| `WebJavaScriptEmitter.prepareLooks` (940–1069) | 130 | 130 | **Flat sequence of phases with one recursive visitor**: rule/keyframe maps (941–951), three closures `addRule`/`noteKeywordProperty`/`visit` (952–1035, `visit` is the recursive walk), then CSS assembly `lookCss` (1036–1052), then `before`/`after` unsafe-CSS segments with a `for (const statement of program.body)` (1053–1067), then `cssOutput` composition (1068). Splittable by extraction. |

### 2.7 Tests / gates that pin internals

- `tests/web/api-contract.test.ts:313` reads the file as text and asserts `assert.match(webEmitter, /visitExtensionRuntimeExpression/u)` and `/visitExtensionRuntimeStatement/u`.
- `scripts/check-runtime-boundary.mjs` reads it as `webEmitterSource` (line 341) and makes **7 assertion sites**: line 706 (a phrase roster for Web-local reactive calls), 1903 (`B-DETACHED-TASK` phrase roster), 1935 (`__velarNoteAsyncWork()` occurrence count ≥ 2 across emitter + runtime), 1941 (`assert not /\bvalue\.(?:target|preventDefault|stopPropagation)\b/`), 1942 (event modifiers), 1960 (`assert not /\bqueueMicrotask\s*\(/`), 2169 (managed async host ABI), 2237 (error-normalization phrase roster). All are **whole-file `includes`/regex over `emitter.ts` alone** — code moved to a sibling module leaves them red unless the gate is taught the family, the way `nodeFamilySource`/`compilerLayer` already do elsewhere.

---

## 3. `packages/web/src/compiler.ts`

**Lines now 1136 — ceiling 1136.** This is the package's composition root.

### 3.1 Top-level structure, in order

| Lines | What |
|---|---|
| 1–22 | imports from `./analyzer.ts`, `./ast.ts`, `./browser-test.ts`, `./editor.ts`, `./elements.ts`, `./emitter.ts`, `./inspection.ts`, `./lexer.ts`, `./look.ts`, `./parser.ts`, `./runtime.ts`, `./semantic.ts`, `./types.ts`, `@velarscript/compiler`, `@velarscript/compiler/extension` |
| 23 | `export const VELAR_WEB_API_VERSION = "0.14"` |
| 24–116 | primitive/Look `ValueType` singletons; `webCanonicalCollectionProjection`; `namedFunction`; `lookBuilder` |
| 117–653 | **the type tables**, one block per `velar/*` surface: globals & `velar/look` exports (139–202), constructors `functionType`/`namedIntrinsic`/`promise`/`object`/`capabilityHandle` (203–223), forms (224–240), `velar/http` (240–299), `velar/storage` + database + storage error classes (295–351), `velar/web` routes/navigation (352–366), `velar/browser` (367–424), `velar/app` (425–431), browser-test controller (432–482), `velar/websocket` (483–534), `velar/realtime` (535–653) |
| **654–877** | `export const webModuleInterfaces: ReadonlyMap<string, ModuleInterface>` (**224 lines**, one entry per `velar/*` module) |
| 879–890 | `moduleInterface` helper |
| 891–949 | documentation tables: `webLookHookDocumentation` (14), `webLookTargetDocumentation` (14), `webKeywordDocumentation` (31) |
| **950–1133** | `export const velarCompilerExtension: CompilerExtension = Object.freeze({...})` (**184 lines**) — `id`, `capabilities`, `lexical`, `parser: Object.freeze(... new VelarWebParser ...)`, `analyzer: Object.freeze(... new VelarWebAnalyzer ...)`, `semantic`, `inspection`, `project`, `inferIntrinsic: inferWebIntrinsic`, `editor` (a ~50-entry completion roster), `modules`, `createEmitter` |
| 1134–1136 | re-exports (`webModuleSource`, `webModuleSources`, `VelarWebRuntimeConfig`, `velarProjectExtension`, `VelarWebConfig`) |

**Exports (6 statements, 3 value names):** `VELAR_WEB_API_VERSION`, `webModuleInterfaces`, `velarCompilerExtension` (+ re-exports).

No classes. **No function over 120 lines** — the mass is data (`webModuleInterfaces` 224, `velarCompilerExtension` 184, ~450 lines of `ValueType` tables). The two multi-line-template candidates my scanner flagged (897–899, 911–913) are markdown fences inside `.join("\n")` string arrays, not runtime bodies.

### 3.2 Coupling

**Imported by:** `packages/web/src/index.ts` and `packages/web/src/host.ts` (`VELAR_WEB_API_VERSION`); `scripts/check-runtime-boundary.mjs:45` (`velarCompilerExtension as velarWebCompilerExtension`); `scripts/surface-inventory.mjs:121, 642`; and **34 test files** (`velarCompilerExtension`, `webModuleInterfaces`, `webModuleSource`, `webModuleSources` — this is the package's public composition, used as the extension in `compile()` calls across `tests/cli`, `tests/compiler`, `tests/core`, `tests/web`).

### 3.3 Gates that pin it

`scripts/check-runtime-boundary.mjs` reads it as `webCompilerSource` (line 340), 2 assertion sites: line 442 (`velar/websocket` must remain client-only) and 1779 (`namedIntrinsic("runtime.parseAsync"` must occur exactly twice). `tests/web/api-contract.test.ts:308` asserts `/parser: Object\.freeze/`, `/analyzer: Object\.freeze/`, `/semantic: velarWebSemanticExtension/`, `/inspection: velarWebInspectionExtension/`, `/project: velarWebProjectEditorExtension/`, `/inferIntrinsic: inferWebIntrinsic/`, `/capabilities: Object\.freeze\(\["web"\]\)/`. `scripts/surface-inventory.mjs:121` reads `VELAR_WEB_API_VERSION` by file path and constant name.

---

## 4. `packages/web/src/parser.ts`

**Lines now 1050 — ceiling 1050.**

### 4.1 Top-level structure, in order

| Lines | What |
|---|---|
| 1–39 | imports: `@velarscript/compiler`, `@velarscript/compiler/extension`, `./ast.ts`, `./lexer.ts`, `./visual-blocks.ts` |
| 40–54 | `span`, `diagnostic`, `recoveredDiagnostic`, `renderBlockSpellings`, `lifecycleHookSpellings`, `componentHeaderShapes`, `reactiveBindingShapes`, `actionHeaderShapes`, `exposeValueStartKinds` |
| **55–604** | `export class VelarWebParser extends Parser` (550 lines) |
| 605–700 | span-shifting helpers: `shiftSourceSpan`, `shiftExpressionSource`, `shiftJsxSyntax`, `shiftLookSyntax`, `shiftKeyframesSyntax` |
| 662–700 | `jsxExpression` |
| **701–896** | `class LookSourceParser` (196 lines, not exported) |
| **898–1032** | `class KeyframesSourceParser` (135 lines, not exported) |
| 1033–1050 | `replaceLookHooks` |

**Exports (1):** `VelarWebParser`.

### 4.2 `VelarWebParser` — 1 field, 8 protected seams, 17 private methods

Field: `insideComponentProps`.

| Group | Members (start, length) | Lines | Field |
|---|---|---|---|
| **Construction & Core seams** | `constructor` (58,11), `parseParameters` (69,56) [protected], `componentPropKeyword` (125,9), `createNestedParser` (134,8) [protected], `matchLifecycleHook` (142,7), `validateExtensionTypeArguments` (149,8) [protected], `parseExtensionNumericLiteral` (157,7) [protected] | 106 | `insideComponentProps` (written in `parseParameters`, read in `componentPropKeyword`) |
| **Statement dispatch** | `parseExtensionStatement` (164,53) [protected], `parseUnsafeExtensionStatement` (217,25) [protected], `namedDeclarationAhead` (242,9), `blockHeaderAhead` (251,23), `exposeItemAhead` (274,4), `parseExtensionImport` (278,22) [protected], `parseUnsafeCssPlacement` (300,15) | 151 | — |
| **Expression dispatch (JSX / Look / keyframes)** | `parseExtensionExpression` (315,51) [protected], `consumeVisualBlock` (366,11), `parseKeyframesExpression` (377,35), `parseJsxEmbedded` (412,36) | 133 | — |
| **Reactive declarations** | `parseStateDeclaration` (448,13), `parseComputedDeclaration` (461,8), `parseResourceDeclaration` (469,8), `parseActionDeclaration` (477,20), `parseWatchDeclaration` (497,13) | 62 | — |
| **Components** | `parseComponent` (510,93) | 93 | — |

### 4.3 `LookSourceParser` (701–896) and `KeyframesSourceParser` (898–1032)

Both are private helper classes with four identical injected fields (`lines`, `blockSpan`, `parseExpression`, `report`) plus a cursor. This is the closest thing in `packages/web` to the compiler's collaborator shape already.

- `LookSourceParser`: fields `lines`, `blockSpan`, `parseExpression`, `report`, `index`; `constructor` (708,11), public `parse()` (719,8), `parseEntries` (727,94), `parseIf` (821,40), `parseLookCondition` (861,32), `lineSpan` (893,4).
- `KeyframesSourceParser`: adds `seenOffsets`, `previousGroupStart`; `constructor` (907,11), public `parse()` (918,37), `parseOffsets` (955,35), `parseEntries` (990,38), `lineSpan` (1028,5).

Both `parse()` methods are called only from within `parser.ts`.

### 4.4 Coupling / runtime strings / functions over 120

**Imported by:** `packages/web/src/compiler.ts:16` only (`new VelarWebParser(tokens, lexicalExtensions)` at 974). **No runtime strings.** **No function over 120 lines** (longest: `parseEntries` 94, `parseComponent` 93, `parseComponent` and `parseEntries` are both single recursive-descent bodies). **Test pin:** `tests/web/api-contract.test.ts:309` asserts `assert.match(webParser, /class VelarWebParser extends Parser/u)`; the same test asserts the *Core* parser must not match `/parse(?:Component|StateDeclaration|ComputedDeclaration|ResourceDeclaration|ActionDeclaration|WatchDeclaration|Jsx)/`.

---

## 5. `packages/web/src/look.ts`

**Lines now 1041 — ceiling 1041.** Pure data + pure functions; **zero imports**, **55 exports**, no class, no function over 120 lines, no runtime strings.

### 5.1 Top-level structure, in order

| Lines | Group |
|---|---|
| 1–92 | unit types & media vocabulary: `LookUnitTypeName`, `LOOK_UNIT_TYPES`, `LOOK_MEDIA_LENGTH_UNITS`, `LOOK_MEDIA_SUBJECTS`, `LOOK_ABSENT_MEDIA_SUBJECTS`, `LOOK_UNITLESS_PROPERTIES`, `LOOK_LENGTH_BUILDERS`, `LOOK_BUILDER_NUMERIC_RANGES`, `LOOK_BORDER_STYLE_NAMES`, `LOOK_ARITHMETIC_HINT`, `LOOK_NUMERIC_TYPE_NAMES`, `LOOK_PUBLIC_TYPE_NAMES`, `LOOK_HOOKS`, `LOOK_TARGETS` |
| 93–214 | builders & design tokens: `LookBuilderSignature`, `LookBuilderResultKind`, `LOOK_BUILDER_SIGNATURES`, `LOOK_BUILDERS`, `LOOK_TOKEN_NAME_PATTERN`, `LOOK_TOKEN_NAME_RULE`, `LOOK_TOKEN_NO_FALLBACK_GUIDANCE`, `isLookTokenName`, `lookTokenReference`, `lookVarReferenceName`, `isLookVarReference` |
| 215–228 | animation vocabulary: `LOOK_ANIMATION_EASINGS`, `LOOK_ANIMATION_DIRECTIONS`, `LOOK_ANIMATION_FILLS`, `LOOK_NON_ANIMATABLE_PROPERTIES` |
| 229–317 | property roster & value kinds: `LookPropertyGroup`, `LOOK_PROPERTY_GROUPS`, `LOOK_PROPERTIES`, `lookPropertyDocumentationKey`, `LookPropertyValueKind`, `propertyKinds`, `LOOK_PROPERTY_VALUE_KINDS`, `cssWideKeywords`, `keywords`, `LOOK_CSS_WIDE_KEYWORDS`, `LOOK_SHARED_METRIC_KEYWORDS`, `LOOK_COLOR_KEYWORDS` |
| 318–378 | CSS-function suggestions: `LookCssFunctionSuggestion`, `lookFilterFunctions`, `LOOK_PROPERTY_CSS_FUNCTIONS`, `LOOK_TRANSITION_PROPERTY_KEYWORDS` |
| 379–475 | keyword combinatorics: `lookValuePairs`, `lookValueOrderings`, `lookValueRepetitions`, and the ~15 per-property keyword arrays (positions, text-decoration, list-style, color-scheme, scroll-snap, overscroll, align/justify, touch-action, background-repeat) |
| **476–663** | `LOOK_PROPERTY_KEYWORDS` (**188 lines**, the largest single declaration) |
| 664–873 | keyword decisions & listings: `LOOK_KEYWORD_DECIDED_KINDS`, `lookOwnKeywords`, `lookPropertyCompletionKeywords`, `lookBuilderSupportsProperty`, `LOOK_KEYWORD_LISTING_LIMIT`, `LOOK_LARGE_KEYWORD_SETS`, `LOOK_PARTIAL_KEYWORD_PROPERTIES`, `LOOK_EXCLUDED_PROPERTIES` |
| 875–999 | naming & shorthands: `cssPropertyName`, `lookDirectShorthands`, `lookShorthandClosure`, `LOOK_SHORTHAND_LONGHANDS`, `lookShorthandOverlap`, `lookShorthandParts` |
| 1000–1041 | `nameDistance`, `nearestLookName` |

### 5.2 Coupling

**Imported by (9 source files + 6 test files):** `packages/web/src/analyzer.ts` (~40 names), `emitter.ts` (6), `compiler.ts` (8), `editor.ts`, `look-static.ts`, `types.ts`, `inspection.ts`, `semantic.ts`, `analysis/look-values.ts`; `tests/web/vocabulary.test.ts`, `tests/web/look-completion.test.ts`, `tests/web/reactive-declarations.test.ts`, `tests/web/look-shorthand-ordering.test.ts`, `tests/web/reactive-graph-and-runtime-abi.slow.test.ts`, `tests/web/look-tokens.test.ts`, `tests/web/look-keywords.test.ts`, `tests/cli/language-server-look-completion.test.ts`, `tests/compiler/emit/generated-module-specifiers.test.ts`, `tests/repo/tour-coverage.slow.test.ts`. `scripts/surface-inventory.mjs:477` names it as the owner of the Look surface.

The tests here import the exported constants directly (`LOOK_PROPERTIES`, `LOOK_PROPERTY_VALUE_KINDS`, `LOOK_EXCLUDED_PROPERTIES`, `LOOK_SHORTHAND_LONGHANDS`, `LOOK_TRANSITION_PROPERTY_KEYWORDS`, `LOOK_NUMERIC_TYPE_NAMES`, `LOOK_PUBLIC_TYPE_NAMES`, `lookShorthandOverlap`, …) — the 55 export names are the contract that a split must preserve (a facade re-export keeps every one of them valid).

---

## 6. `packages/node/src/server-analyzer.ts`

**Lines now 1236 — ceiling 1236.**

### 6.1 Top-level structure, in order

| Lines | What |
|---|---|
| 1–51 | imports: `@velarscript/compiler/extension`, `./route-overlap.ts`, `./route-pattern.ts`, `./route-shape.ts`, `./serve-problem-analysis.ts`, `./server-ast.ts`, `./server-types.ts` |
| 52–148 | hint encode/decode + composition types: `routeHintPrefix`, `routeResultHintPrefix`, `routeCaptureHintPrefix`, `responseHeadersType`, `sseEventType`, `sseSendType`, `ComposedRoute`, `ComposedFallback`, `ServerAlias`, `ServeCombinator`, `serveCombinators`, `ComposedServer`, `NodeResponseMetadata`, `NodeResponseValueType`, `RouteParameterSource`, `RouteParameterKind`, `OpenApiSchema`, and the six exported hint codecs `routeParameterHint`/`parseRouteParameterHint`/`routeResultHint`/`parseRouteResultHint`/`routeCaptureHint`/`parseRouteCaptureHint` |
| **149–711** | `export class VelarNodeAnalyzer extends VelarNodeProblemAnalyzer` (563 lines) |
| 713–904 | `uploadType`, **`export function inferNodeIntrinsic` (715–904, 190 lines)** |
| 906–1018 | response-shape predicates: `literalStatus`, `nodeResponseValue`, `nodeOutcomeValue`, `scalarKind`, `isNamedDataRecord`, `isData`, `isResponseShape`, `requireResponseValue`, `isHttpOutcome`, `isRouteResult`, `isResponsePolicyResult` |
| 1019–1168 | OpenAPI schema derivation: `openApiResponseSchema`, `openApiResponseContentTypes`, `openApiResponseStatus`, `enumValueSchema`, `openApiSchema`, `openApiObjectSchema` |
| 1169–1236 | composition & collision messages: `moduleServerAlias`, `describeComposedOrigin`, `describeComposedRoute`, `describeRouteCollision`, `describeFallbackCollision`, `prefixedRoutePath` |

**Exports (10):** `RouteParameterSource`, `RouteParameterKind`, `routeParameterHint`, `parseRouteParameterHint`, `routeResultHint`, `parseRouteResultHint`, `routeCaptureHint`, `parseRouteCaptureHint`, `VelarNodeAnalyzer`, `inferNodeIntrinsic`.

### 6.2 The class: 11 fields, 1 public override, 4 protected seams, 14 private methods

Fields (150–168): `contextualRouteParameters`, `routeInputs`, `moduleServers`, `moduleServerAliases`, `moduleServeCombinators`, `stableAliases`, `moduleProgram`, `routePatternValues`, `importedRoutePatternValues`, `routePatterns`, `nodeModulePath`.

| Group | Members (start, length) | Lines | Fields read/written |
|---|---|---|---|
| **Entry & module scan** | `constructor` (169,9), `analyze` (178,35) [public override] | 44 | `nodeModulePath`, `importedRoutePatternValues`, `moduleServers`, `moduleServerAliases`, `moduleServeCombinators`, `stableAliases`, `moduleProgram`, `routePatternValues`, `routePatterns` |
| **Core seams** | `predeclareExtensionStatement` (213,6), `analyzeExtensionStatement` (219,6), `contextualFunctionParameterDefault` (225,21), `inferExtensionExpression` (246,7) | 40 | `routeInputs`, `contextualRouteParameters` |
| **Route patterns / captures** | `staticPattern` (253,9), `routePath` (262,8), `boundRoutePathType` (270,22), `routeCaptureType` (292,9), `recordRouteCaptureHint` (301,9) | 57 | `routePatterns`, `routePatternValues` |
| **Server composition** | `analyzeServer` (310,50), `recordRoute` (360,75), `composedItems` (435,60), `resolveComposedServer` (495,32), `aliasBindingIsStable` (527,12), `resolvesTo` (539,5) | 234 | `moduleServeCombinators`, `moduleServers`, `moduleServerAliases`, `stableAliases`, `moduleProgram` |
| **Handlers** | `analyzeNotFound` (544,24), `analyzeResponse` (568,32), `analyzeRoute` (600,111) | 167 | `routeInputs`, `contextualRouteParameters`, `routePatterns` |

`analyzeRoute` is 111 lines — under the cap, but the largest single member.

### 6.3 Called from outside the class

`analyze` (base entry), and the 4 protected seams (base `Analyzer` in `packages/compiler/src`). All 14 private methods are internal. The class is constructed at `packages/node/src/compiler.ts:28` import and used in `velarNodeCompilerExtension`.

### 6.4 Coupling / runtime strings / functions over 120

**Imported by:** `packages/node/src/compiler.ts:28` (`inferNodeIntrinsic`, `VelarNodeAnalyzer`) and `packages/node/src/server-emitter.ts:15` (`parseRouteCaptureHint`, `parseRouteParameterHint`, `parseRouteResultHint`). Nothing in `tests/` imports it directly. **No runtime strings** (`packages/node/src` is in `rawTemplateScopes`; runtime is 32 `.js` files under `packages/node/runtime/`).

| Function over 120 | Lines now | Ceiling | Shape |
|---|---|---|---|
| `inferNodeIntrinsic` (715–904) | 190 | 190 | **Single control-flow**: one destructuring line, then `switch (intrinsic.name)` to the end. Arms split by module family (`serve.*`, `http.*`, `fs.*`, `process.*`, `terminal.*`, `websocket.*`). |

---

## 7. `packages/node/src/compiler.ts`

**Lines now 894 — ceiling 894.** Node's composition root; no class, **no function over 120 lines**, no runtime strings in TS.

### 7.1 Top-level structure, in order

| Lines | What |
|---|---|
| 1–40 | imports: `@velarscript/compiler`, `@velarscript/compiler/extension`, `./modules/serve.ts`, `./runtime-sources.generated.ts`, `./server-analyzer.ts`, `./server-ast.ts`, `./server-documentation.ts`, `./server-emitter.ts`, `./server-inspection.ts`, `./server-lexer.ts`, `./server-parser.ts`, `./server-semantic.ts`, `./server-types.ts` |
| 41–52 | `VELAR_NODE_API_VERSION = "0.17"`, `VELAR_NODE_HOST_MODULE`, primitive `ValueType` singletons |
| 54–116 | type constructors: `promise`, `functionType`, `namedIntrinsic`, `capabilityHandle`, `object`, `moduleInterface` |
| 117–463 | the type tables, one block per surface: `velar/serve` server/problem/SSE/responses (117–216), providers & route inputs (216–235), middleware & lifecycle (236–265), `velar/server-test` (252–265), `velar/fs` (266–276), `velar/process` (277–298), `velar/http` + terminal (299–359), HTTP transport & error classes (360–395), `velar/websocket` (396–462) |
| **464–701** | `export const nodeModuleInterfaces` (**238 lines**) |
| 702–730 | `nodeModuleSources`, `nodeModuleDependencies` |
| 731–764 | `nodeModules`, `sharedPlatformModules`, `isNodeModule`, `isNodeOnlyModule`, `nodeModuleWebGuidance`, `nodeModuleDiagnostic` |
| **765–890** | `export const velarNodeCompilerExtension: CompilerExtension` (**126 lines**) |
| 891 | `export const velarCompilerExtension = velarNodeCompilerExtension` |

**Exports (13 statements, 10 names):** `VELAR_NODE_API_VERSION`, `VELAR_NODE_HOST_MODULE`, `nodeModuleInterfaces`, `nodeModuleSources`, `nodeModuleDependencies`, `isNodeModule`, `isNodeOnlyModule`, `nodeModuleDiagnostic`, `velarNodeCompilerExtension`, `velarCompilerExtension`, plus a re-export of `VELAR_PROCESS_HOST_RUNTIME`.

### 7.2 Coupling and gate pins

**Imported by:** `packages/node/src/index.ts`, `scripts/check-runtime-boundary.mjs:46`, `scripts/surface-inventory.mjs:122, 643`, and ~9 test files (`tests/cli/build-and-serve-bounds`, `tests/cli/language-server-framework-semantic-tokens`, `tests/cli/language-server-server-semantic-tokens`, `tests/cli/language-server-standard-api-documentation`, `tests/cli/library-artifact-target`, `tests/cli/module-resolution-diagnostics`, `tests/compiler/diagnostics/node-global-guidance`, `tests/compiler/semantic/module-graph-containers.slow`, `tests/core/hash`, `tests/core/module-enum-surface`, `tests/core/standard-library`, `tests/node/host-transport`).

`scripts/check-runtime-boundary.mjs` reads it as `nodeCompilerSource` (line 342) — **6 assertion sites**: 445 (must own the WebSocket server surface), 451 (must not own `velar/server`), 1311 (`export { VELAR_PROCESS_HOST_RUNTIME } from "./runtime-sources.generated.ts"` must be present), 1521 (host-runtime composition phrase roster), 1709 (`["velar/http", [VELAR_NODE_HOST_MODULE, "velar/binary"]]`), 1842 (an exact `namedIntrinsic("runtime.parseAsync", …)` line) plus 1780 (occurrence count exactly 4). `tests/web/api-contract.test.ts:277` reads it and asserts `/id: "@velarscript\/node"/`, `/\["velar\/serve", moduleInterface/`, `/\["velar\/fs", VELAR_NODE_FS_MODULE_SOURCE\]/`.

---

## 8. `packages/cli/src/project.ts`

**Lines now 2653 — ceiling 2730** (already 77 below).

### 8.1 Top-level structure, in order

| Lines | Group |
|---|---|
| 1–75 | imports (24 statements: 20 siblings, `@velarscript/compiler`, `node:fs/promises`, `node:module`, `node:path`) |
| 76–86 | re-exports (`VelarPackageSubpath`, `VelarSourcePackage`, …) |
| 87–245 | limits + the public result/option shapes: `MAX_PROJECT_RESOURCES`, `MAX_JSON_RESOURCE_BYTES`, `ProjectModule`, `ProjectFailure`, `ProjectNotice`, `ProjectResource`, `ProjectOwnedResourcePackage`, `ProjectResult`, `ProjectCompilationStats`, `CompileProjectOptions`, `LoadedModule`, `PendingModule` |
| 246–300 | `projectModuleResult` (12), `projectImportKey` (3), `ModuleResolutionDiagnostic`, `JavaScriptDependencyContext`, `resolutionRecorder` (9), `compileProject` (10) |
| **302–841** | **`compileProjectEntries` (540)** |
| 843–1141 | dependency & incremental machinery: `checkJavaScriptDependency` (45), `standardModuleListing` (4), `migratedStandardPackageDiagnostic` (15), `importedReactiveAssignmentDiagnostics` (24), `affectedModules` (79), `artifactInputs` (15), `dependencyFirstCompilationGroups` (12), `stronglyConnectedPaths` (59), `moduleDependencies` (16) |
| 1143–1420 | initialization-cycle diagnostics: `INITIALIZATION_CYCLE_DIAGNOSTIC`, `MODULE_RESOLUTION_DIAGNOSTIC_PREFIX`, **`appendInitializationCycleDiagnostics` (1164–1375, 212)**, `originModule` (20), `uninitializedModuleName` (9) |
| **1422–1562** | **`moduleInterfaceIdentity` (141)** |
| **1564–1734** | **`createAnalysisContext` (171)** |
| **1736–1904** | **`resolvedModuleInterface` (148)**, `stableSourcePackageInterface` (15) |
| 1906–2070 | resources: `resolveJsonResource` (13), `resolveResourcePackage` (22), `authorizeJsonResourcePath` (11), `ProjectJsonResourceReadOperations`, `readProjectJsonResource` (54), `validateJsonResourceText` (6), `projectModuleBoundaries` (14), `escapesRoot` (3), `normalizeModulePath` (3) |
| 2072–2358 | cross-module type metadata: `importHiddenTypeMetadata` (32), `collectTypeIdentities` (55), `standardTypeOwnerIndexes`, `standardTypeOwners`, `importReachableStandardTypeMetadata` (32), `importInterface` (**120 — exactly at the cap**) |
| 2360–2653 | type rewriting: `renameClass` (7), `mapClassInfo` (33), `renameType` (84), `resolveKnownNominals` (88), `expandKnownAliases` (52), `uniqueFailures` (9), `uniqueNotices` (9) |

**Exports (18 statements, 15 names):** `MAX_JSON_RESOURCE_BYTES`, `ProjectModule`, `ProjectFailure`, `ProjectNotice`, `ProjectResource`, `ProjectOwnedResourcePackage`, `ProjectResult`, `ProjectCompilationStats`, `CompileProjectOptions`, `projectImportKey`, `compileProject`, `compileProjectEntries`, `moduleInterfaceIdentity`, `ProjectJsonResourceReadOperations`, `readProjectJsonResource` (+ two type re-exports).

No classes.

### 8.2 Coupling

**Imported by 30+ files in `packages/cli/src`** — `module-assets.ts`, `resource-output.ts`, `frozen-package-output.ts`, `build-input-boundary.ts`, `project-semantic.ts`, `library-artifact-bundle.ts`, `library-artifact-build.ts`, `test-runner.ts`, `project-failure.ts`, `npm-frozen-artifact.ts`, `standalone-output-transaction.ts`, `project-session-inputs.ts`, `cli.ts`, `language-server.ts`, `browser-test-runner.ts`, `npm.ts`, `ownership-graph.ts`, `dev-server.ts`, … Most import only the types `ProjectResult` / `ProjectModule` / `ProjectResource` / `ProjectFailure` plus the 3-line `projectImportKey`; the heavy machinery (`compileProjectEntries`, `moduleInterfaceIdentity`, `readProjectJsonResource`) has a handful of callers.

### 8.3 Runtime strings

**None.** No `String.raw`, no multi-line template. (`packages/cli/src` is **not** in `rawTemplateScopes` — see §10.5.)

### 8.4 Functions over 120 lines

| Name | Lines now | Ceiling | Shape |
|---|---|---|---|
| `compileProjectEntries` (302–841) | 540 | 541 | **Flat sequence of phases with one very large loop.** Phase 1: option normalization + ~30 `const` accumulators (310–372). Phase 2: `enqueue` closure + the module-discovery walk `for (let cursor = 0; cursor < pending.length; cursor += 1)` (**389–666, 278 lines** — the single largest block). Phase 3: incremental reuse + `for (const group of compilationGroups)` compilation (667–769). Phase 4: module-interface resolution `for (const group of compilationGroups)` (784–797). Phase 5: `framework?.host.validateProject` (798–811) and the result literal (812–841). Splittable by phase; the discovery loop is one control-flow body of its own. |
| `appendInitializationCycleDiagnostics` (1164–1375) | 212 | 218 | **Flat sequence** of four independent stages, each behind its own relevance guard: four closures (1171–1183), relevance scan + early return (1188–1195), static-dependency collection (1197–1220), Tarjan SCC over static edges (1221–1240), then component-member grouping and the diagnostic emission (1242–1375). Splittable by extraction. |
| `moduleInterfaceIdentity` (1422–1562) | 141 | 141 | **Flat sequence**: `node`/`typeMap`/`names`/`types` helper closures (1426–1433), then ten independent `const <segment> = node(...)` hash segments (`namedTypes`, `namedTypeReadonlyFields`, `namedTypeIdentities`, `namedTypeBases`, `genericTypes`, `enums`, `classes`, `extensionExports`, …), then one `return node("module-interface", [...])`. Each segment is an independent step. |
| `createAnalysisContext` (1564–1734) | 171 | 171 | **Mixed**: 13 accumulator maps (1577–1589), two small `for` loops over `loaded` and `artifactInterfaces` (1590–1604), then one large `for (const dependency of module.inspection.dependencies)` (1605–1717, 113 lines) that merges each dependency's interface, then the return literal (1718–1734). |
| `resolvedModuleInterface` (1736–1883) | 148 | 148 | **Mixed**: a cache check + 12 map copies (1745–1772), a large `for (const dependency of module.inspection.dependencies)` (1775–1860, 86 lines), then a nominal-resolution tail (1861–1883). |

`importInterface` (2239–2358) is exactly 120 lines — not a violation, but one line from becoming one.

### 8.5 Gates that pin it

`scripts/check-runtime-boundary.mjs` reads it as `projectCompilerSource` (line 718) — **10 assertion sites**, including line 760 ("project compilation does not request shared compiler runtime modules"), 1831 (`Type<T>` must survive rename, nominal resolution and alias expansion) and 1837 (`Record<T>` must preserve nested value types). These are whole-file `includes` over `project.ts` alone. The gate also walks *every* file under `packages/cli/src` at line 477 (`cliSources`) for a separate set of rules that are directory-scoped and therefore split-safe.

No test imports a private-ish export from `project.ts`; no `as any` reach. Several `tests/cli` files build `{ … } as unknown as ProjectResult` fixtures (`frozen-package-output.test.ts`, `standalone-output-metadata-limits.test.ts`, `standalone-output-recovery.test.ts`, `embedded-source-build.test.ts`, `standalone-output-safety.test.ts`) — those pin the *shape* of the exported `ProjectResult` interface, not the module layout.

---

## 9. `packages/cli/src/cli.ts`

**Lines now 1722 — ceiling 1792.** The `velar` binary entry (`#!/usr/bin/env node`, `packages/cli/package.json` `"bin": {"velar": "./dist/cli.js"}`). **Zero exports.**

### 9.1 Top-level structure, in order

| Lines | Group |
|---|---|
| 1–88 | shebang + 56 import statements (49 siblings, `@velarscript/compiler`, `create-velar`, four `node:` modules) |
| 89–201 | argument shapes + build-output helpers: `CommandArguments`, `BuildLibraryArguments`, `BUILD_OUTPUT_CONCURRENCY`, `mapBuildOutputs` (18), `writeCliStandaloneBuildOutput` (22), `FormatArguments`, `DevArguments`, `ServeArguments`, `TestArguments`, `PreviewArguments`, `RunArguments`, `ReproArguments`, `DeploymentVerificationArguments`, `GraphArguments` |
| **202–876** | **`main` (675)** |
| 878–1274 | build-output writers: `writeGenericDirectoryApplication` (59), `writeFrameworkProductionApplication` (46), `writeNodeProductionApplication` (97), `packageFrameworkOutput` (9), `BuildOutputReplacement`, `prepareBuildStaging` (24), `assertReplaceableBuildOutput` (19), `isBuildOutputDirectory` (12), `writeCompiled` (68), `rewriteVelarPackageImports` (26) |
| 1276–1601 | argument parsers (12 functions, 5–46 lines each): `parseFormatArguments`, `parseCommandArguments`, `parseBuildLibraryArguments`, `parseReproArguments`, `parsePackageArguments`, `parseDevArguments`, `parseServeArguments`, `parseTestArguments`, `parseRunArguments`, `helpRequested`, `parseSingleOptionalInput`, `parseGraphBound`, `parseGraphArguments`, `parsePreviewArguments`, `parseDeploymentVerificationArguments` |
| 1603–1698 | display & help: `displayInput`, `displayPath`, `printHelp` (33), `commandNames`, `printCommandHelp` (47) |
| 1699–1722 | `dependencyResultMessage` (17) + the top-level `try { process.exitCode = await main(process.argv.slice(2)) }` |

### 9.2 Functions over 120 lines

| Name | Lines now | Ceiling | Shape |
|---|---|---|---|
| `main` (202–876) | 675 | 693 | **Flat sequence of independent guarded command arms.** 22 early-return blocks, each `if (command === "…") { … return n }`: `--help`/`-h` (205), `help` (214), `--version`/`-v` (227), per-command help (239), `lsp` (244), `skill` (260), `graph` (277), `create` (308), `install`/`add`/`remove`/`update` (324), `verify` (340), `preview` (362), `verify-deployment` (378), `run` (407), `fix` (432), `repro` (486), `build-library` (526), an unknown-command guard (558), `format` (564), `dev` (651), `serve` (673), `test` (688); then a shared tail (730–875) for `check`/`build`/`package`: parse → compile → report → `check` exit → `package` → single-file output → framework/node production output → generic build. Every arm is independently extractable; the tail is one more. |

### 9.3 Gates that pin it

`scripts/check-runtime-boundary.mjs` reads it as `cliSource` (line 724) — **16 assertion sites**, several of them *ordering* and *slicing* assertions that depend on the file's own layout:

- 993–994: `cliSource.indexOf("await recoverInterruptedBuilds(normalizedOutput, staging.claim, isBuildOutputDirectory)") > cliSource.indexOf("const authorization = await validateBuildOutputTarget")` — a **byte-offset ordering rule within one file**.
- 1001–1003: `const buildOutputOwnershipSource = cliSource.slice(cliSource.indexOf("async function isBuildOutputDirectory"), cliSource.indexOf("async function writeCompiled"))` — a **region carved out by two function names in source order**, then phrase-checked at 1017.
- 868, 925, 1014, 1019, 1139 (`hasCallWithLeadingArguments(cliSource, "prepareBuildStaging", ["outputDirectory","replacement","project"])`), 1142 (regex over `assertBuildInputsOutsideOutput`), 1150.

`tests/web/api-contract.test.ts:352` reads `cli.ts` as one of the `cliFrameworkHostSources` and asserts `assert.doesNotMatch(source, /@velarscript\/web/u)` for each, plus a joined-source roster (`require.resolve(\`${name}/host\`)`, `VELAR_FRAMEWORK_HOST_PROTOCOL_VERSION`, `createFrameworkArtifacts`) and negative assertions (`jsx-tag|component-attribute|native-attribute|…`). The joined form is split-tolerant; the per-file `doesNotMatch` is too. The `check-runtime-boundary` slice/ordering rules are not.

Tests otherwise invoke `cli.ts` as a child process (`spawnSync(process.execPath, ["packages/cli/src/cli.ts", …])`) in `tests/core/standard-library.test.ts`, `tests/web/typed-boundaries.test.ts`, `tests/web/inline-css.test.ts`, `tests/desktop/desktop.test.ts`, `tests/server/server-port-zero.test.ts`, `tests/cli/language-server-standard-api-documentation.test.ts` — path-pinned, not internals-pinned.

### 9.4 Runtime strings

**None** in `cli.ts`.

---

## 10. `packages/cli/src/language-server.ts`

**Lines now 1743 — ceiling 1743.**

### 10.1 Top-level structure, in order

| Lines | Group |
|---|---|
| 1–69 | 15 imports (`./canonical-path.ts`, `./format-guard.ts`, `./host-error.ts`, `./ownership-graph.ts`, `./project-semantic.ts`, `./project-session-diagnostics.ts`, `./project-session.ts`, `./project.ts`, `./standard-api-documentation.ts`, `./standard-modules.ts`, `./version.ts`, `./workspace-index.ts`, `@velarscript/compiler`, `node:path`, `node:url`) |
| 70–131 | protocol shapes + module state + limits: `RpcMessage`, `TextDocument`, `Position`, `Range`, `ContentChange`, `VELAR_LANGUAGE_SERVER_PROTOCOL_VERSION = 5`, `PositionEncoding`, mutable module-level `activePositionEncoding` / `confinedWorkspaceRoot` / `confinedCanonicalRoot`, `MAX_LSP_MESSAGE_BYTES`, `MAX_LSP_RESULT_ITEMS`, `MAX_LSP_TEXT_CHARS`, `MAX_EMITTED_JAVASCRIPT_CHARS`, `MAX_OWNERSHIP_GRAPH_NODES`, `MAX_OWNERSHIP_GRAPH_EDGES`, `semanticTokenTypes`, `semanticTokenModifiers`, `nonVelarDocumentResults` |
| 132–326 | documentation & completion tables: `keywordDocumentation` (132–203, 72 lines of `@main`/`@context`/`@dispose`/`@iterate`/… prose with embedded ```` ```velar ```` fences), `builtinTypeDocumentation` (204–221), `corePreludeCompletionDetail`, `permanentNamespaceCompletionDetail`, `corePreludeDocumentation` (14), `standardModuleCompletionDetail`, `importableStandardModules` (11), `coreCompletionItems`, `completionItemsFor` (10, exported), `extensionDocumentation` (11) |
| **327–1206** | **`runLanguageServer` (880)** |
| 1208–1743 | 44 pure helpers, 3–50 lines each — see the grouping below |

**Exports (3):** `VELAR_LANGUAGE_SERVER_PROTOCOL_VERSION`, `completionItemsFor`, `runLanguageServer`.

The helper tail (1208–1743) groups as: **diagnostics mapping** (`boundedDiagnostics`, `oversizedDiagnosticsFallback`, `lspDiagnostic`, `lspAdvisory`, `lspNotice`, `boundedDiagnosticSpan`, `clipLspText`); **hover** (`hover`, 50); **locations & edits** (`lspLocation`, `lspRange`, `sourceFor`, `workspaceEdit`, `sameLspRange`); **semantic tokens** (`semanticTokenData`); **code actions** (`quickFixes`); **inlay hints** (`projectInlayHints`); **kind mapping** (`lspSymbolKind`, `lspCompletionKind`); **URI/path confinement** (`pathOf`, `rawPathOf`, `isVelarDocument`, `authorizedPathOf`, `requestedWorkspaceRoots`, `configuredWorkspaceRoot`, `configuredCanonicalRoot`, `withinWorkspaceRoot`, `projectRelativeGraphPath`); **text/position arithmetic** (`applyContentChanges`, `offsetAt`, `positionAt`, `requestedPositionEncoding`, `lspSourcePosition`, `codePointCount`, `codeUnitOffsetAt`, `nextLineStart`, `lineEndAt`, `wordAt`, `fullRange`, `workspacePosition`); **transport plumbing** (`requestKey`, `isRpcMessage`, `mapBounded`).

### 10.2 Inside `runLanguageServer` (327–1206)

| Lines | What |
|---|---|
| 330–350 | server state: `configuredCanonical`, `documents`, `sessions` (`VelarProjectSessions`), `ownershipGraphs`, `workspaceIndex`, `pendingRequests`, `cancelledRequests`, `diagnosticUris`, `buffer`, `queue`, `diagnosticTask`, `workspaceIndexTask`, `workspaceIndexFailure`, `shuttingDown`, `finish`, `exitRequested` |
| 352–515 | 12 closures: `send`, `overrides`, `projectSnapshotFor`, `projectFor`, `queueWorkspaceIndex`, `waitForWorkspaceIndex`, `publish` (56), `schedulePublish`, `refreshWorkspaceProjects`, `finishRequest`, `respond`, `respondError` |
| **516–1150** | **`handle` (635)** |
| 1152–1202 | `process.stdin.on("data", …)` framing loop |
| 1203–1206 | shutdown join |

### 10.3 Functions over 120 lines

| Name | Lines now | Ceiling | Shape |
|---|---|---|---|
| `runLanguageServer` (327–1206) | 880 | 882 | **Flat sequence**: state, 12 closures, `handle`, the stdin framing loop, the shutdown join. Everything except `handle` is small. |
| `runLanguageServer.handle` (516–1150) | 635 | 635 | **Single control-flow**: a short prologue (cancellation check 517–520, shutdown guard 521–524, `params` extraction 525, non-Velar-document short-circuit 526–533) then one `switch (message.method)` over **28 LSP method arms** running to 1149. Arm sizes: `initialize` 535–589 (55), `initialized` 590, `shutdown` 595, `exit` 599, `$/cancelRequest` 604, `didOpen` 606–621, `didChange` 622–634, `didSave` 635–640, `didClose` 641–651, `didChangeWatchedFiles` 652–700 (49), `velar/workspaceRescan` 701–708, `velar/workspaceSearch` 709–749 (41), `velar/ownershipGraph` 750–860 (**111**), `velar/emittedJavaScript` 861–910 (50), `workspace/symbol` 911–961 (51), `completion` 962–993, `hover` 994–1000, `definition` 1001–1010, `references` 1011–1024, `documentHighlight` 1025–1041, `prepareRename` 1042–1052, `rename` 1053–1065, `documentSymbol` 1066–1079, `signatureHelp` 1080–1093, `inlayHint` 1094–1102, `semanticTokens/full` 1103–1111, `codeAction` 1112–1123, `formatting` 1124–1149. Splittable per arm / per capability. |

### 10.4 Coupling and test pins

**Imported by:** `packages/cli/src/language-server-bundle-entry.ts` (`runLanguageServer`); `tests/cli/language-server-standard-api-documentation.test.ts`, `tests/compiler/analysis/derived-vocabulary.test.ts`, `tests/compiler/lexer/contextual-keyword-roster.test.ts` — all three import **`completionItemsFor`**, an export that exists mainly so tests can reach the completion roster without starting a server. That is the one internals-ish reach into this file. No `as any` reaches. `scripts/check-runtime-boundary.mjs` does not read it by path.

### 10.5 Runtime strings

**None.** The four multi-line-template candidates my scanner flagged (157–161, 168–172, 179–185, 192–198) are markdown ```` ```velar ```` fences inside `keywordDocumentation`'s `.join("\n")` string arrays — documentation prose, not runtime JavaScript.

---

## 11. `packages/cli/src/project-semantic.ts`

**Lines now 1296 — ceiling 1333.** **No function over 120 lines** (largest: `projectCompletionsAt` 511–586, 76; `projectSemanticTokens` 320–384, 65). ~80 functions, all small.

### 11.1 Top-level structure, in order

| Lines | Group |
|---|---|
| 1–39 | 7 imports (`./lsp/semantic-token-roles.ts`, `./project.ts`, `./stable-order.ts`, `./standard-api-documentation.ts`, `@velarscript/compiler`, `node:path`) |
| 40–123 | public result shapes: `ProjectLocation`, `ProjectTextEdit`, `ProjectDocumentSymbol`, `ProjectWorkspaceSymbol`, `ProjectSignature`, `ProjectCompletion`, `ProjectSemanticToken`, `ProjectSyntaxDocumentation`, `ProjectCompletionContext`, `ProjectRename`, `ProjectRenameFailure`; internal `LocalTarget`, `ExportTarget`, `ProjectTarget`, `MemberTarget` |
| 124–153 | rename-restriction messages: `bindingRenameRestrictionMessage`, `memberRenameRestrictionMessage`, `memberOwnerKind` |
| 154–258 | **definition / references / rename**: `projectDefinitionAt`, `projectReferencesAt`, `projectPrepareRenameAt`, `projectRenameAt` |
| 260–319 | **symbols**: `projectDocumentSymbols`, `projectWorkspaceSymbols`, `byName` |
| 320–384 | **semantic tokens**: `projectSemanticTokens` |
| 385–510 | **documentation & symbol lookup**: `projectSyntaxDocumentationAt`, `projectSymbolAt`, `projectExpressionAt`, `projectUnresolvedValueReferenceAt`, `projectMemberSymbolAt`, `projectMemberDocumentationAt`, `standardMemberDocumentation`, `bareMemberOwner` |
| 511–715 | **completion**: `projectCompletionsAt`, `completionOwnerTarget`, `projectCompletionContextAt`, `extensionCompletionAt`, `objectFieldContextAt`, `memberAccessAt` |
| 716–755 | **signature help**: `projectSignatureAt`, `memberCallAt` |
| 756–1155 | **target resolution** (the largest cluster, ~400 lines): `targetAt`, `memberTargetAt`, `accessibleMemberTargetAt`, `memberTargetForExpression`, `memberTargetForReference`, `memberTargetForOwner`, `ownerTarget`, `declaredOwnerByIdentity`, `findDeclaredMember`, `memberLocations`, `renameableMember`, `extensionRenameProtection`, `memberRenameCollides`, `memberRenameEdits`, `memberContractTargets`, `memberContractRootOwner`, `classHierarchyOwners`, `classOwnerForMember`, `baseClassOwner`, `directClassMember`, `classDescendsFrom`, `exportedTarget`, `importedModule`, `enumMemberAt`, `enumMemberTargetAt`, `enumMemberLocations`, `enumMemberRenameCollides` |
| 1157–1296 | **utilities**: `escapeRegExp`, `exportedLocations`, `localLocations`, `referencesFor`, `renameCollides`, `scopeHasName`, `renameSelectionAt`, `importForSymbol`, `moduleAt`, `locationOf`, `uniqueLocations`, `uniqueTextEdits`, `renameable`, `contains`, `wordSpanAt`, `callAt` |

**Exports (28 statements, 27 names)** — 11 interfaces/types and 16 `projectXxxAt`/`projectXxx` functions.

### 11.2 Coupling and test pins

**Imported by:** `packages/cli/src/language-server.ts:56` (the whole `projectXxx` roster) and **12 test files**, each importing a specific exported entry point:

| Test | reaches |
|---|---|
| `tests/web/api-contract.test.ts` | `projectSignatureAt` (+ reads the file as text at line 350) |
| `tests/cli/source-packages.test.ts` | `projectDefinitionAt` |
| `tests/cli/module-contracts.test.ts` | `projectSignatureAt` |
| `tests/cli/semantic-method-symbols.test.ts` | `projectSymbolAt` |
| `tests/cli/language-server-look-completion.test.ts` | completion entry points |
| `tests/cli/language-server-standard-api-documentation.test.ts` | documentation entry points |
| `tests/cli/language-server-imported-function-semantic-tokens.test.ts` | `projectSemanticTokens` |
| `tests/cli/project-semantics.test.ts` | multiple |
| `tests/cli/language-server-embedded-javascript-semantic-tokens.test.ts` | `projectSemanticTokens` |
| `tests/cli/generic-classes-editor.test.ts` | `projectSymbolAt` |
| `tests/cli/language-server-framework-semantic-tokens.test.ts` | `projectSemanticTokens`, `ProjectSemanticToken` |
| `tests/cli/language-server-css-keyword-semantic-tokens.test.ts` | `projectSemanticTokens`, `ProjectSemanticToken` |

All are public exports; a facade re-export preserves every one. `tests/web/api-contract.test.ts:350` additionally reads the file as text for the `doesNotMatch(/@velarscript\/web/)` boundary rule. **No runtime strings.**

---

## 12. `packages/cli/src/typescript-declarations.ts`

**Lines now 1736 — ceiling 1759.**

### 12.1 Top-level structure, in order

| Lines | Group |
|---|---|
| 1–28 | 7 imports (`./installed-package.ts`, `./typescript-declaration-source.ts`, `@velarscript/compiler`, `node:fs/promises`, `node:module`, `node:path`, `node:url`) |
| 29–63 | `TypeScriptDeclarationBridge` (exported), `DeclarationDirection`, `oppositeDirection`, primitive `ValueType` singletons (`unknownType`, `unsupportedType`, `nullType`, `stringType`, `numberType`, `boolType`, `bytesType`, four buffer types), `MAX_TYPESCRIPT_DECLARATION_DEPTH` |
| 65–160 | **entry resolution**: `loadTypeScriptDeclarations` (exported, 53), `firstDeclarationEntry` (22), `declarationFileCandidates` (9), `isDeclarationFile` (5) |
| 150–429 | **declaration graph**: `DeclarationReexport`, `DeclarationImport`, **`loadTypeScriptDeclarationGraph` (161–323, 163)**, `emptyDeclarationBridge`, `declarationReexports`, `declarationImports`, `LocalDeclarationExport`, `localDeclarationExports`, `resolveDeclarationReexport`, `insideRoot`, `renameDeclarationExport`, `renameDeclarationType` |
| **430–739** | **`parseTypeScriptDeclarations` (310, exported)** |
| 741–1009 | **classes**: `ConstantDeclaration`, `constantDeclarations`, `TypeScriptClassDeclaration`, `readClassDeclarations`, **`parseClassDeclaration` (823–1008, 186)** |
| 1010–1124 | **parameters**: `parseRestElementType`, `withoutThisParameter`, `parseParameters`, `parseClassConstructorParameters` |
| **1125–1319** | **types**: **`parseTsType` (1125–1268, 144)**, `objectType`, `setObjectField` |
| 1320–1419 | **signature readers**: `readFunctionSignature`, `readMethodSignature`, `readAccessorSignature`, `skipWhitespace`, `closingQuote` |
| 1420–1657 | **source scanning**: `stripDeclarationComments`, `ambientBlockPattern`, `excludeAmbientBlocks`, `matchingDelimiter`, `topLevelTerminator`, `memberHeadPattern`, `memberContinues`, `splitFields`, `splitTopLevel`, `balanced` |
| 1658–1736 | **package exports resolution**: `exportedTypes`, `selectPackageExport`, `findTypesTarget`, `firstStringTarget`, `packageNameOf`, `stringValue`, `unique` |

**Exports (3):** `TypeScriptDeclarationBridge`, `loadTypeScriptDeclarations`, `parseTypeScriptDeclarations`.

### 12.2 Functions over 120 lines

| Name | Lines now | Ceiling | Shape |
|---|---|---|---|
| `parseTypeScriptDeclarations` (430–739) | 310 | 310 | **Flat sequence of independent phases**: byte-size guard (439), comment/ambient stripping (443), interface collection loop over `interfacePattern` (449–460), alias collection loop over `aliasPattern` (462–470), `resolveInterface` closure + resolution loop (471–507), eight accumulator maps (509–516), then the per-export scanning phases to 738. Splittable by extraction. |
| `parseClassDeclaration` (823–1008) | 186 | 186 | **Mixed**: header guard (830), ten accumulator maps/sets (834–844), three closures `reject`/`duplicate`/`reserved` (846–859), then **one large `for (const raw of splitFields(declaration.body))`** (860–960, 101 lines) that dispatches per member kind, then `materializeAccessors` closure (961–983) and the assembly tail (984–1008). |
| `parseTsType` (1125–1268) | 144 | 144 | **Single control-flow**: a normalization prologue (1133–1134) then a long chain of mutually exclusive `if` recognizers — `readonly`, union split, `[]`, the four typed-array names, `generic`, `arrow`, `{…}`, string literal, numeric literal, `true`/`false`, and the primitive-name terminals. Effectively a hand-written `switch`; splits by syntax family rather than by step. |
| `loadTypeScriptDeclarationGraph` (161–323) | 163 | 173 | **Flat sequence** whose body is almost entirely the nested `load` closure: realpath + root containment (162–163), `ownModules`/`cache`/`visiting`/`sourceBudget` (169–172), **`load` (174–319, 146)**, then `const bridge = await load(entryPath, 0)` and the return (321–322). |
| `loadTypeScriptDeclarationGraph.load` (174–319) | 146 | 155 | The recursive per-declaration-file loader: cache/cycle guard, read, budget, `parseTypeScriptDeclarations`, then re-export and import resolution loops. One recursive body with two sequential loops. |

### 12.3 Coupling / runtime strings / test pins

**Imported by:** `packages/cli/src/project.ts` (`loadTypeScriptDeclarations`, `TypeScriptDeclarationBridge`); `tests/cli/package-graph-input-snapshot.test.ts` (`loadTypeScriptDeclarations`), `tests/cli/typescript-declaration-bridge.test.ts` and `tests/cli/typescript-declarations.test.ts` (both `loadTypeScriptDeclarations` + `parseTypeScriptDeclarations`). All public. **No runtime strings** — the four multi-line-template candidates my scanner flagged (1205–1222, 1418–1425, 1507–1530, 1592–1630) are backticks inside regex character classes and warning strings (e.g. `/^['"`]/u`), not templates. **No `check-runtime-boundary` assertions** on this file.

---

## 13. `packages/cli/src/browser-test-runner.ts`

**Lines now 1212 — ceiling 1218.** **The only file in the four packages that still holds a multi-line runtime body in TypeScript.**

### 13.1 Top-level structure, in order

| Lines | Group |
|---|---|
| 1–39 | 21 imports (13 siblings, `@velarscript/compiler`, `@velarscript/compiler/framework-host`, `node:child_process`, `node:fs/promises`, `node:os`, `node:path`, `node:url`, `playwright`, `velar/web-test`) |
| 40–53 | `browserTestHostGuidance`, `BrowserEngine`, `BrowserEngineSelection`, `browserTypes`, `defaultBrowserTestTimeoutMs`, `browserTestWorkerEnvironment`, `browserPerformanceRuntimeKey` |
| **54–269** | **`const browserPerformanceInitScript = String.raw\`…\`` — a 216-line browser-page runtime** |
| 271–315 | `BrowserTestRunnerOptions` (exported), `BrowserTestLimits`, `class BrowserTestInterrupted extends Error` (284–292), `class BrowserTestRunTimedOut extends Error` (294–299), `browserTestLimits` (15) |
| 316–384 | `runBrowserTests` (exported, 33), `superviseBrowserTests` (19), `browserTestWorkerOptions` (18) |
| **385–686** | **`runBrowserTestsInWorker` (302)** |
| 696–775 | reporting & lifecycle: `announceToSupervisor`, `boundedBrowserTestBody`, `settleWorkerWork`, `installFrameworkRuntime`, `installBrowserPerformanceRuntime` |
| 776–832 | test-entry compilation: `BrowserTestEntry`, `compileBrowserTest`, `writeBrowserTestEntry`, `installBrowserCompilerRuntime` |
| **833–1078** | **`installBrowserRuntime` (833–1073, 241)**, `removeBrowserRuntime` |
| 1079–1174 | timing: `BrowserInteractionTiming`, `measureBrowserInteraction`, `navigationTiming`, `interactionTiming`, `timingRecord`, `boundedTiming` |
| 1175–1218 | `buildProject`, `discoverBrowserTestFiles`, `stackOf` |

**Exports (4):** `BrowserEngine`, `BrowserEngineSelection`, `BrowserTestRunnerOptions`, `runBrowserTests`.

### 13.2 Runtime strings

**`packages/cli/src/browser-test-runner.ts:54–269` — `browserPerformanceInitScript = String.raw\`…\``, 216 lines** of browser-page JavaScript (an IIFE that captures native `Object`/`Number`/`Array`/`Math`/`Map`/`Promise`/`Reflect`/`performance`/`document`/`EventTarget`/`Event`/`Element`/`DOMRectReadOnly`/`CSSStyleDeclaration` descriptors, then defines `timings`, `scroll`, `box`, `style`, `prepare`, `finish`, `cancel` on a symbol key). It interpolates one value: `${JSON.stringify(browserPerformanceRuntimeKey)}` at line 57.

Crucially, `scripts/check-runtime-boundary.mjs`'s `rawTemplateScopes` (lines 513–520) lists **`compiler`, `core`, `desktop`, `web`, `node`, `server`** — **`packages/cli/src` is not in the list**, so this literal is not refused today and there is no `packages/cli/runtime/` directory. The comment at 503–512 says "Every package root D115 P3 covers is scanned, which is all of them," and `COMPILER_SOURCE_RAW_TEMPLATE_ALLOWLIST` is `new Set([])` "empty on purpose".

This is the **only** `String.raw` in `packages/web/src`, `packages/node/src`, `packages/cli/src`, `packages/core/src`.

### 13.3 Functions over 120 lines

| Name | Lines now | Ceiling | Shape |
|---|---|---|---|
| `runBrowserTestsInWorker` (385–686) | 302 | 305 | **Mixed**: prologue (392–419: limits, framework contract guard, file discovery, temp dirs, six mutable handles) then **one enormous `try { … } finally { … }`** (420–674) that is the whole run — build, serve, launch browser, per-file loop, teardown — then a cleanup tail (675–685). The `try` body is one control-flow block; the phases inside it are extractable. |
| `installBrowserRuntime` (833–1073) | 241 | 241 | **Flat**: three small closures (`locator`, `storageArea`, `installFrameworkBeforeOpen`) plus `mockedRoutes`/`frameworkRuntime` state (841–852), then **one `Object.freeze({...})` object literal holding 30 async methods** (853–1071), then one assignment to `globalThis[runtimeKey]`. The 30 methods group cleanly: navigation (`open`, `reload`, `currentPath`), interaction (`click`, `fill`, `select`, `press`, `scroll`), query (`text`, `attribute`, `box`, `style`, `namespace`, `count`, `visible`), waiting (`waitFor`, `waitForText`), viewport (`viewport`), timings (`timings`, `animation`, `measureClick`, `measureFill`, `measurePress`), storage (`storageGet`, `storageSet`, `storageRemove`, `storageClear`), network (`networkRespond`, `networkClear`), framework (`frameworkInvoke`). |

### 13.4 Coupling and test pins

**Imported by:** `packages/cli/src/cli.ts` (type `BrowserEngineSelection`), `tests/cli/browser-lifecycle.test.ts` (`runBrowserTests`). `tests/web/api-contract.test.ts:349` reads it as text for the `doesNotMatch(/@velarscript\/web/)` framework-host boundary rule. `scripts/check-runtime-boundary.mjs` does not read it by path.

---

## 14. Per-package notes

### 14.1 `packages/web` — 29 files, 14 857 lines

**Existing directory under `src/`:** `analysis/` only (5 files, 1171 lines).

| Module group | Home today | Compiler's equivalent directory |
|---|---|---|
| Look vocabulary tables (properties, keywords, builders, shorthands) | `look.ts` (1041) | `analysis/vocabulary.ts` + `analysis/collections/operations.ts` (pure rosters more than one cluster reads) |
| Look static evaluation | `look-static.ts` (409) | `analysis/flow/facts.ts` (a side store the analyzer carries) |
| Look numeric folding / length-percentage slots | `analysis/look-values.ts` (285) ✅ | `analysis/expressions/operators.ts` |
| Watch/cycle reactive paths | `analysis/watch-cycles.ts` (420) ✅ | `analysis/flow/narrowing.ts` |
| Reactive name collection | `analysis/reactive-names.ts` (189) ✅ | `analysis/declarations/references.ts` |
| `publicConfig` | `analysis/public-config.ts` (150) ✅ | `analysis/modules/interfaces/declarations.ts` |
| Component refusal messages | `analysis/component-guidance.ts` (127) ✅ | `analysis/expressions/guidance.ts` |
| **JSX analysis** | inside `analyzer.ts` (787 lines) ❌ | `analysis/expressions/*` |
| **Look analysis** | inside `analyzer.ts` (765 lines) ❌ | `analysis/collections/*` |
| **Component/lifecycle analysis** | inside `analyzer.ts` (275 lines) ❌ | `analysis/classes/*` |
| **JSX emission** | inside `emitter.ts` (280 lines) ❌ | `emit/expressions.ts`, `emit/classes.ts` |
| **Look/CSS emission** | inside `emitter.ts` (174) + top-level (180) ❌ | `emit/statements.ts`, `emit/javascript.ts` |
| **Runtime-import selection** | inside `emitter.ts` (131) ❌ | `emit/runtime-imports.ts`, `emit/helper-names.ts` |
| Look/keyframes source parsing | `LookSourceParser`, `KeyframesSourceParser` in `parser.ts` ❌ (not exported) | `parser/statements/*`, `parser/patterns.ts` |
| Web AST / constructs | `ast.ts` (370) | `ast/` (compiler has `ast.ts` 1410, itself allowlisted) |
| Semantic index | `semantic.ts` (263) | `semantic/` |
| Editor completions | `editor.ts` (399) | (compiler: `extension.ts` + `language-guidance.ts`) |
| Lexer | `lexer.ts` (670), `css-tokens.ts` (444), `visual-blocks.ts` (37) | `lexer/` |
| Types / elements / host / config | `types.ts`, `elements.ts`, `host.ts`, `project-config.ts`, `inspection.ts` | `types/`, `contracts.ts` |
| Runtime bodies | `packages/web/runtime/*.js` (40+ files) ✅ R2c landed | `packages/compiler/runtime/` |

The five existing `analysis/` collaborators are **pure functions**, not host-interface collaborators: only `public-config.ts` declares a host (`PublicConfigTypeHost`). The compiler's pattern (a named `…Host` interface per collaborator, satisfied by one live-reading object built in the root) is not yet in use in `packages/web`.

### 14.2 `packages/node` — 19 files, 4330 lines

**Existing directory under `src/`:** `modules/` (1 file, `serve.ts`, 51 lines).

| Module group | Home today | Compiler's equivalent |
|---|---|---|
| Server analysis (routes, composition, handlers) | `server-analyzer.ts` (1236) ❌ | `analysis/` |
| Node intrinsic typing | `inferNodeIntrinsic` in `server-analyzer.ts` (190) ❌ | `analysis/calls/intrinsics.ts` |
| OpenAPI schema derivation | tail of `server-analyzer.ts` (~150) ❌ | `analysis/modules/interfaces/tables.ts` |
| Route-hint codecs | head of `server-analyzer.ts` (~70) ❌ | `contracts.ts` |
| Problem analysis | `serve-problem-analysis.ts` (162), `serve-call-analysis.ts` (109) ✅ | `analysis/advisories/` |
| Route patterns / overlap / shape | `route-pattern.ts` (286), `route-overlap.ts` (64), `route-shape.ts` (38) ✅ | `analysis/matching.ts`, `analysis/match-coverage.ts` |
| Parser / lexer / emitter / semantic / inspection / documentation | `server-parser.ts` (317), `server-lexer.ts` (85), `server-emitter.ts` (269), `server-semantic.ts` (100), `server-inspection.ts` (22), `server-documentation.ts` (94) ✅ | `parser/`, `lexer/`, `emit/`, `semantic/` |
| Module interface tables + extension composition | `compiler.ts` (894) ❌ | `index.ts` + `extension.ts` (the compiler's facade pair) |
| Runtime bodies | `packages/node/runtime/*.js` (32 files) ✅ R2d landed | `packages/compiler/runtime/` |

D115 §三 says explicitly: "`server-analyzer/parser/emitter` 保持但归入 `analysis/`、`emit/`."

### 14.3 `packages/cli` — 143 files, 39 111 lines

**Existing directory under `src/`:** `lsp/` (1 file, `semantic-token-roles.ts`, 62 lines). The package is already the most decomposed by file count — the six allowlisted files are 10 362 lines, 27 % of the package, and the other 137 files are all under 800.

| Module group | Home today | D115 §三 target | Compiler's equivalent |
|---|---|---|---|
| Command dispatch | `main` in `cli.ts` (675) ❌ | — | `index.ts` (facade) |
| Build-output writing | `cli.ts` 878–1274 + `build-output-*.ts` (7 files) partly ✅ | — | `emit/` |
| Argument parsing / help | `cli.ts` 1276–1698 ❌ | — | — |
| Project graph discovery | `compileProjectEntries` 389–666 ❌ | `project/graph.ts` | `analysis/modules/imports.ts` |
| Module-interface resolution & identity | `moduleInterfaceIdentity`, `resolvedModuleInterface`, `createAnalysisContext`, `importInterface` ❌ | `project/interfaces.ts` | `analysis/modules/interfaces/{assembly,declarations,tables}.ts` |
| Incremental reuse | `affectedModules`, `dependencyFirstCompilationGroups`, `stronglyConnectedPaths` ❌ | `project/incremental.ts` | `analysis/flow/loops.ts` |
| Entries / resources | `readProjectJsonResource`, `resolveResourcePackage`, `projectModuleBoundaries` ❌ | `project/entries.ts` | `analysis/modules/exports.ts` |
| Cycle & resolution diagnostics | `appendInitializationCycleDiagnostics` (212) ❌ | `project/diagnostics.ts` | `analysis/advisories/` |
| Type renaming / nominal resolution | `renameType`, `resolveKnownNominals`, `expandKnownAliases`, `collectTypeIdentities` ❌ | — | `types/{unification,assignability,model}.ts` |
| LSP transport & handler switch | `runLanguageServer`/`handle` ❌ | `lsp/transport.ts` + per-capability | `parser.ts`'s six-phase `parseStatementBody` dispatcher |
| LSP capability arms | `handle`'s 28 arms ❌ | `lsp/{hover,completion,rename,semantic-tokens,diagnostics}.ts` | `analysis/statements/*` |
| LSP mapping helpers | `language-server.ts` 1208–1743 ❌ | `lsp/*` | `semantic/queries.ts` |
| Semantic-token role table | `lsp/semantic-token-roles.ts` ✅ | `lsp/semantic-tokens.ts` | `semantic/symbols.ts` |
| Project semantic queries | `project-semantic.ts` (1296) ❌ | — | `semantic/{declarations,references,documentation,queries}.ts` |
| TypeScript `.d.ts` bridge | `typescript-declarations.ts` (1736) ❌ | — | `types/from-syntax.ts` + `parser/type-syntax.ts` |
| Browser test orchestration | `browser-test-runner.ts` (1212) ❌ | — | — (no compiler equivalent; the page runtime belongs in a `packages/cli/runtime/`) |
| Runtime bodies | **`browser-test-runner.ts:54–269` still in TS** ❌ | `packages/cli/runtime/*.js` | `packages/compiler/runtime/` |

### 14.4 `packages/core` — 19 files, 987 lines

Nothing over budget. Already at the D115 §三 target: `index.ts` (286, the aggregator), `interfaces/<module>.ts` (18 files: `task`, `binary`, `types`, `worker`, `log`, `validation`, `text`, `math`, `random`, `url`, `time`, `async`, `json`, `id`, `test`, `hash`), `standard-module-route.ts`, `runtime-sources.generated.ts` (68), and `packages/core/runtime/*.js` (22 files). No P4 work. `scripts/check-runtime-boundary.mjs` makes ~12 assertions against `packages/core/src/index.ts` and one against `packages/core/src/interfaces` as a directory (line 846: "no `velar/*` interface table was found") — the directory-scoped form is the split-safe shape.

---

## 15. The 28 allowlisted files

Legend: ✅ = in P4 scope (web/node/cli/core), ⬜ = out of scope (compiler = P2 done / composition root; tests = P5).

| # | Path | Lines now | Ceiling | Concern groups found | Collaborator-module names the compiler's pattern suggests |
|---|---|---|---|---|---|
| 1 ✅ | `packages/web/src/analyzer.ts` | 5153 | 5154 | entry passes (141); declarations/dispatch (242); components+ownership (275); reactive names (158); retired accessors (86); watch/cycles (248); keyed rebuild (98); **Look (765)**; keyframes (66); **JSX (787)**; type predicates (99); routes (44); inference plumbing (227); + 1660 lines of top-level tables & helpers | Composition root `analyzer.ts` (43 fields, 15 seams, 2 dispatchers, host ctors) + `analysis/jsx/{elements,attributes,children,keys,security}.ts`, `analysis/look/{entries,values,builders,tokens,conditions,vocabulary}.ts`, `analysis/keyframes.ts`, `analysis/components/{declaration,contracts,lifecycle,ownership}.ts`, `analysis/reactivity/{bindings,derived,retired-accessors}.ts`, `analysis/watch-cycles.ts` (exists), `analysis/keyed-rebuild.ts`, `analysis/routes.ts`, `analysis/renderable.ts`, `analysis/calls/intrinsics.ts` (`inferWebIntrinsic`), `analysis/watch-subject.ts` (`renderWatchSubject`), `analysis/web-types.ts`; hosts `JsxAnalysisHost`, `LookAnalysisHost`, `ComponentAnalysisHost`, `ReactiveNamesHost`, `WatchCycleHost` |
| 2 ✅ | `packages/web/src/emitter.ts` | 1365 | 1365 | entry/accessors (48); runtime-import selection (131); extension walking (76); statement+expression dispatch (129); Look emission (174+180 top-level); components (119); **JSX (280)**; CSS lowering helpers (179) | `emitter.ts` keeps the 12 seams + `emit/{jsx,components,look,look-css,runtime-imports,javascript}.ts`; hosts `JsxEmitHost`, `LookEmitHost`, `ComponentEmitHost` (state half must stay live accessors — `cssOutput`/`webOutput`/`needs*` are set by families and read by `emit()`) |
| 3 ✅ | `packages/web/src/compiler.ts` | 1136 | 1136 | type constructors (60); per-surface `ValueType` tables (~450); `webModuleInterfaces` (224); documentation tables (59); `velarCompilerExtension` (184) | `modules/{look,web,http,storage,browser,forms,files,realtime,websocket,app,config,browser-test}.ts` (one table per `velar/*`, mirroring `packages/core/src/interfaces/`); `documentation.ts`; `compiler.ts` stays the frozen extension literal |
| 4 ✅ | `packages/web/src/parser.ts` | 1050 | 1050 | `VelarWebParser` construction+Core seams (106); statement dispatch (151); expression dispatch (133); reactive declarations (62); components (93); span shifting (96); `LookSourceParser` (196); `KeyframesSourceParser` (135) | `parser/statements/{components,reactive,imports,unsafe-css}.ts`, `parser/expressions/{jsx,look,keyframes}.ts`, `parser/look-source.ts`, `parser/keyframes-source.ts`, `parser/spans.ts`; hosts `ComponentParserHost`, `LookParserHost` |
| 5 ✅ | `packages/web/src/look.ts` | 1041 | 1041 | units & media (92); builders & tokens (122); animation (14); property roster & value kinds (89); CSS-function suggestions (61); keyword combinatorics (97); **`LOOK_PROPERTY_KEYWORDS` (188)**; keyword decisions & listings (210); naming & shorthands (125); nearest-name (42) | `look/{units,media,builders,tokens,animation,properties,keywords,keyword-sets,css-functions,shorthands,naming}.ts` with `look.ts` re-exporting all 55 names (the `types.ts` → `types/` re-export pattern) |
| 6 ✅ | `packages/node/src/server-analyzer.ts` | 1236 | 1236 | hint codecs (97); entry+module scan (44); Core seams (40); route patterns/captures (57); **server composition (234)**; handlers (167); `inferNodeIntrinsic` (190); response predicates (113); OpenAPI schemas (150); collision messages (68) | Composition root `analysis/server-analyzer.ts` + `analysis/{routes,composition,handlers,captures}.ts`, `analysis/calls/intrinsics.ts` (`inferNodeIntrinsic`), `analysis/openapi/{schema,responses}.ts`, `analysis/response-shapes.ts`, `contracts.ts` (route-hint codecs), `analysis/collisions.ts`; hosts `RouteAnalysisHost`, `ServerCompositionHost` |
| 7 ✅ | `packages/node/src/compiler.ts` | 894 | 894 | version consts (12); type constructors (63); per-surface tables (347); `nodeModuleInterfaces` (238); sources/dependencies (29); module predicates (34); `velarNodeCompilerExtension` (126) | `modules/{serve,fs,process,terminal,http,websocket,server-test}.ts` (`modules/serve.ts` already exists), `module-policy.ts`; `compiler.ts` stays the frozen extension literal |
| 8 ✅ | `packages/cli/src/project.ts` | 2653 | 2730 | public shapes (159); **`compileProjectEntries` (540)**; dependency+incremental (299); cycle diagnostics (233); interface identity (141); analysis context (171); interface resolution (163); resources (165); type metadata (287); type rewriting (294) | D115 §三: `project/{graph,interfaces,incremental,entries,diagnostics}.ts`; plus `project/{types,resources,identity}.ts`. Compiler equivalents: `analysis/modules/imports.ts`, `analysis/modules/interfaces/{assembly,declarations,tables}.ts`, `analysis/flow/loops.ts`, `types/{unification,assignability}.ts` |
| 9 ✅ | `packages/cli/src/cli.ts` | 1722 | 1792 | argument shapes (113); **`main` — 22 command arms + shared build tail (675)**; build-output writers (397); argument parsers (326); help/display (96); tail (24) | `commands/{check,build,package,format,dev,serve,test,run,fix,repro,graph,create,install,verify,preview,verify-deployment,build-library,lsp,skill,help}.ts` + `build/{generic,framework,node,single-file,staging}.ts` + `arguments.ts` + `help.ts`; `cli.ts` becomes the dispatcher (the compiler's `parseStatementBody` six-phase shape) |
| 10 ✅ | `packages/cli/src/language-server.ts` | 1743 | 1743 | protocol shapes & limits (62); documentation/completion tables (195); **`runLanguageServer` (880)** of which **`handle` (635) = 28 LSP arms**; mapping helpers (536) | D115 §三: `lsp/{transport,hover,completion,rename,semantic-tokens,diagnostics}.ts`; plus `lsp/{definition,references,symbols,code-actions,inlay-hints,formatting,ownership-graph,emitted-javascript,workspace,positions,documentation}.ts`. Compiler equivalent: `semantic/queries.ts` ("the four questions a position asks of a finished index") |
| 11 ✅ | `packages/cli/src/project-semantic.ts` | 1296 | 1333 | public shapes (84); rename restrictions (30); definition/references/rename (105); symbols (60); semantic tokens (65); documentation & lookup (126); completion (205); signature help (40); **target resolution (400)**; utilities (140) | `semantic/{definition,references,rename,symbols,tokens,documentation,completion,signature,targets,members,enums,locations}.ts` — a direct mirror of the compiler's `semantic/{symbols,declarations,references,documentation,indexer,queries}.ts` |
| 12 ✅ | `packages/cli/src/typescript-declarations.ts` | 1736 | 1759 | entry resolution (96); declaration graph (280); **`parseTypeScriptDeclarations` (310)**; classes (269); parameters (115); types (195); signature readers (100); source scanning (238); package exports (79) | `typescript/{entry,graph,declarations,classes,parameters,types,signatures,scanning,package-exports}.ts` — mirrors the compiler's `types/from-syntax.ts` + `parser/type-syntax.ts` + `lexer/` split |
| 13 ✅ | `packages/cli/src/browser-test-runner.ts` | 1212 | 1218 | **`String.raw` page runtime (216)**; options/limits/errors (45); entry+supervisor (69); **`runBrowserTestsInWorker` (302)**; reporting/lifecycle (80); test-entry compilation (57); **`installBrowserRuntime` — 30-method API object (241)**; timing (96); build/discover (44) | `packages/cli/runtime/browser-performance.js` (the `String.raw` body, via `scripts/generate-runtime-sources.mjs` + a `cli` entry in `RUNTIME_PACKAGES` and `rawTemplateScopes`); `browser-test/{run,worker,supervisor,entry,runtime-api,timings,discovery}.ts`; `runtime-api.ts` splits into navigation / interaction / query / waiting / storage / network / framework families |
| 14 ⬜ | `packages/compiler/src/analyzer.ts` | 3167 | 3242 | **composition root** — 166 fields, 17 host ctors (953), 66 protected seams (551), 2 dispatchers, 25 public readers | Already landed (R1f). Line cap explicitly waived by the D115 revision; segment budgets live in `compiler-architecture.md` |
| 15 ⬜ | `packages/compiler/src/ast.ts` | 1410 | 1410 | Core AST node shapes + construct roster | D115 §三: `ast/nodes/{statements,expressions,types,patterns}.ts`, `ast/constructs.ts`, `ast/walk.ts` (P2 remainder) |
| 16 ⬜ | `packages/compiler/src/emitter.ts` | 1613 | 1613 | class + 32 seams forwarding to `emit/` | Already split into `emit/` (10 modules); the remainder is the seam surface |
| 17 ⬜ | `packages/compiler/src/parser.ts` | 1449 | 1449 | class + 6-phase `parseStatementBody` dispatcher | Already split into `parser/` (10 modules) |
| 18 ⬜ | `tests/compiler/analysis/class-and-core-correctness.test.ts` | 1167 | 1173 | — | P5 |
| 19 ⬜ | `tests/compiler/analysis/performance-runtime.slow.test.ts` | 987 | 987 | — | P5 |
| 20 ⬜ | `tests/compiler/types/bounded-generics-and-dispose.slow.test.ts` | 916 | 916 | — | P5 |
| 21 ⬜ | `tests/desktop/desktop-runtime.test.ts` | 1742 | 1742 | — | P5 |
| 22 ⬜ | `tests/desktop/desktop-worker.test.ts` | 1124 | 1124 | — | P5 |
| 23 ⬜ | `tests/node/node-platform.slow.test.ts` | 2809 | 2809 | — | P5 |
| 24 ⬜ | `tests/node/node-server-framework.test.ts` | 1012 | 1012 | — | P5 |
| 25 ⬜ | `tests/web/reactive-graph-and-runtime-abi.slow.test.ts` | 879 | 882 | — | P5 |
| 26 ⬜ | `tests/web/reactivity.slow.test.ts` | 1014 | 1018 | — | P5 |
| 27 ⬜ | `tests/web/runtime.slow.test.ts` | 1214 | 1214 | — | P5 |
| 28 ⬜ | `tests/web/surface.test.ts` | 1464 | 1464 | — | P5 |

---

## 16. The 34 allowlisted functions

| # | Key | Lines now | Ceiling | Shape | Concern groups inside | Suggested collaborator module |
|---|---|---|---|---|---|---|
| 1 ✅ | `packages/cli/src/browser-test-runner.ts#installBrowserRuntime` (833–1073) | 241 | 241 | flat: 3 closures + one 30-method `Object.freeze` literal | navigation, interaction, query, waiting, viewport, timings, storage, network, framework | `browser-test/runtime-api/{navigation,interaction,query,waiting,timings,storage,network,framework}.ts` |
| 2 ✅ | `…#runBrowserTestsInWorker` (385–686) | 302 | 305 | one `try/finally` around the whole run | limits+discovery, temp layout, build, serve, browser launch, per-file loop, teardown | `browser-test/worker/{prepare,serve,launch,run,teardown}.ts` |
| 3 ✅ | `packages/cli/src/cli.ts#main` (202–876) | 675 | 693 | **flat** — 22 independent `if (command === …) return` arms + shared build/check tail | one arm per command | `commands/<command>.ts` (20 modules) + `commands/build-tail.ts` |
| 4 ✅ | `packages/cli/src/dev-server.ts#runDevServer` (75–319) | 245 | 245 | flat: state + 6 closures (`scheduleRebuild`, `syncPackageWatchers`, `rebuild`, request handler, watcher, listen) | rebuild scheduling, npm/package watching, HTTP request routing, file watching, framework processes | `dev/{state,rebuild,watchers,request-handler,processes}.ts` |
| 5 ✅ | `packages/cli/src/language-server.ts#runLanguageServer` (327–1206) | 880 | 882 | flat: state, 12 closures, `handle`, stdin framing, shutdown | session state, publish/diagnostics, workspace index, transport | `lsp/{session,transport,diagnostics,workspace}.ts` |
| 6 ✅ | `…#runLanguageServer.handle` (516–1150) | 635 | 635 | **single control-flow** — `switch (message.method)`, 28 arms | lifecycle, documents, workspace, graph/emit, symbols, completion, hover, navigation, rename, tokens, actions, formatting | `lsp/{lifecycle,documents,workspace,ownership-graph,emitted-javascript,symbols,completion,hover,navigation,rename,semantic-tokens,code-actions,formatting}.ts` |
| 7 ✅ | `packages/cli/src/mechanical-fixer.ts#applyProjectMechanicalFixes` (115–237) | 123 | 146 | mixed: setup + one `while (passes < maximumPasses)` (135–211) + a remaining-diagnostics tail | pass loop, write failures, remaining-diagnostic report | `fix/{passes,report}.ts` |
| 8 ✅ | `packages/cli/src/npm.ts#resolveBrowserNpm` (211–333) | 123 | 141 | mixed: setup + one `while (queue.length > 0)` bundling loop (238–295) + import-map assembly tail | anchors, prebundle queue, package list, import map | `npm/{anchors,prebundle,import-map}.ts` |
| 9 ✅ | `packages/cli/src/ownership-graph.ts#buildOwnershipGraphScoped` (373–595) | 223 | 223 | mixed: bounds+6 closures (`checkpoint`, `addNode`, `addEdge`, …) + **two** `for (const module of includedModules)` loops (449–488, 489–569) + a finalization tail | bounds, node/edge accumulation, module pass 1, module pass 2, finalization | `graph/{bounds,accumulator,modules,symbols,capabilities}.ts` |
| 10 ✅ | `packages/cli/src/project.ts#appendInitializationCycleDiagnostics` (1164–1375) | 212 | 218 | **flat** — 4 stages behind independent relevance guards | relevance scan, static edges, Tarjan SCC, diagnostic emission | `project/diagnostics/{relevance,edges,components,report}.ts` |
| 11 ✅ | `…#compileProjectEntries` (302–841) | 540 | 541 | **flat phases**, one 278-line discovery loop (389–666) | options, discovery walk, incremental reuse, compilation groups, interface resolution, framework validation, result | `project/{options,graph,incremental,entries,interfaces}.ts` |
| 12 ✅ | `…#createAnalysisContext` (1564–1734) | 171 | 171 | mixed: 13 accumulators + one 113-line dependency loop | imports, named types, generics, aliases, enums, classes, extension imports | `project/interfaces/analysis-context.ts` |
| 13 ✅ | `…#moduleInterfaceIdentity` (1422–1562) | 141 | 141 | **flat** — 10 independent hash segments + one return | named types, readonly fields, identities, bases, generics, enums, classes, extension exports | `project/interfaces/identity.ts` |
| 14 ✅ | `…#resolvedModuleInterface` (1736–1883) | 148 | 148 | mixed: 12 map copies + one 86-line dependency loop + nominal-resolution tail | own interface, dependency merge, nominal resolution | `project/interfaces/resolution.ts` |
| 15 ✅ | `packages/cli/src/typescript-declarations.ts#loadTypeScriptDeclarationGraph` (161–323) | 163 | 173 | flat wrapper around `load` | root containment, budget, recursion entry | `typescript/graph.ts` |
| 16 ✅ | `…#loadTypeScriptDeclarationGraph.load` (174–319) | 146 | 155 | recursive body with 2 sequential loops | cache/cycle, parse, re-exports, imports | `typescript/graph-loader.ts` |
| 17 ✅ | `…#parseClassDeclaration` (823–1008) | 186 | 186 | mixed: 10 accumulators + one 101-line member loop + accessor materialization | fields, methods, statics, getters, accessors, constructor, base | `typescript/classes.ts` |
| 18 ✅ | `…#parseTsType` (1125–1268) | 144 | 144 | **single control-flow** — a chain of mutually exclusive syntax recognizers | readonly, union, array, typed arrays, generics, arrow, object, literals, primitives | `typescript/types.ts` |
| 19 ✅ | `…#parseTypeScriptDeclarations` (430–739) | 310 | 310 | **flat phases** | interfaces, aliases, interface resolution, exports, classes, constants, runtime exports | `typescript/declarations.ts` split by phase |
| 20 ⬜ | `packages/compiler/src/embedded-javascript-editor.ts#embeddedJavaScriptEditorTokens` | — | 337 | out of scope (P2/compiler) | — | — |
| 21 ⬜ | `packages/compiler/src/index.ts#compileUnchecked` | — | 171 | out of scope | — | — |
| 22 ⬜ | `packages/compiler/src/lexer.ts#Lexer.lex` (217–…) | — | 277 | out of scope | — | — |
| 23 ⬜ | `packages/create/src/templates.ts#desktopTemplate` | — | 143 | out of scope (`packages/create`) | — | — |
| 24 ⬜ | `packages/desktop/src/build.ts#buildDesktopApplication` | — | 144 | out of scope (`packages/desktop`) | — | — |
| 25 ✅ | `packages/node/src/server-analyzer.ts#inferNodeIntrinsic` (715–904) | 190 | 190 | **single control-flow** — `switch (intrinsic.name)` | per module family: serve, http, fs, process, terminal, websocket | `analysis/calls/intrinsics/{serve,http,fs,process,terminal,websocket}.ts` |
| 26 ✅ | `packages/web/src/analyzer.ts#VelarWebAnalyzer.analyzeComponent` (3158–3279) | 122 | 122 | mixed: prologue + one 76-line section-dispatch loop | props, handle type, render, mounted, cleanup, expose, computed, resource, action, watch | `analysis/components/{declaration,sections}.ts` |
| 27 ✅ | `…#VelarWebAnalyzer.analyzeNativeJsxAttribute` (4532–4683) | 152 | 152 | **flat** — independent guarded arms | events, bind, style, look, class, attribute value, spelling, advisories | `analysis/jsx/attributes/{events,bind,visual,native,spelling}.ts` |
| 28 ✅ | `…#inferWebIntrinsic` (254–436) | 183 | 183 | **single control-flow** — `switch (intrinsic.name)` | web, http, storage, forms, realtime, config, browser, files | `analysis/calls/intrinsics/{web,http,storage,forms,realtime,config,browser,files}.ts` |
| 29 ✅ | `…#renderWatchSubject` (1297–1421) | 125 | 125 | **single control-flow** — `switch (expression.kind)` | identifiers, members, indexes, calls, literals, operators | `analysis/watch-subject.ts` |
| 30 ✅ | `packages/web/src/editor.ts#completeWebProject` (145–265) | 121 | 121 | **flat** — 8 independent context guards (`look.kind === "property" \| "value" \| "target" \| "hook" \| "media"`, JSX tag, JSX attribute) | Look completion, JSX tag completion, JSX attribute completion | `editor/{look-completion,jsx-completion}.ts` |
| 31 ✅ | `packages/web/src/emitter.ts#WebJavaScriptEmitter.emitJsxCode` (664–800) | 137 | 137 | mixed: component branch (56) + element setup + attribute loop + child loop | component element, native element, attributes, children | `emit/jsx/{component,element,attributes,children}.ts` |
| 32 ✅ | `…#WebJavaScriptEmitter.prepareLooks` (940–1069) | 130 | 130 | **flat phases** + one recursive `visit` | rule collection, keyframe rules, CSS assembly, unsafe-CSS segments | `emit/look/{collect,css,segments}.ts` |
| 33 ⬜ | `tests/acceptance/browser.acceptance.ts#acceptBrowser` (166–…) | — | 217 | out of scope (P5) | — | — |
| 34 ⬜ | `tests/desktop/desktop-runtime.test.ts#bridge.invoke#2` | — | 197 | out of scope (P5) | — | — |

**In-scope totals: 13 files (10 362 + 5153 + … = 20 546 lines across web/node/cli), 27 functions.** `packages/core` contributes nothing.

---

## 17. Cross-cutting facts a splitter needs

1. **Two source-text pin families will break on a naive move.**
   - `tests/web/api-contract.test.ts` reads 9 files as text (`web/{compiler,parser,analyzer,semantic,inspection,emitter,editor}.ts`, `cli/*`) with single-file `readFile`. Its own `compilerLayer(entry, directory)` helper (lines 294–301) is the split-tolerant form and is used for the compiler layers only.
   - `scripts/check-runtime-boundary.mjs` reads `web/compiler.ts` (2 sites), `web/emitter.ts` (7), `node/compiler.ts` (6), `cli/project.ts` (10), `cli/cli.ts` (16), `cli/npm.ts` (2), `cli/dev-server.ts` (2). Two `cli.ts` rules are structural rather than textual: an **`indexOf` ordering comparison** (993–994) and a **region slice between two function names** (1001–1003). The gate already demonstrates the split-tolerant alternative in two places: `nodeFamilySource(family)` / `packageFamilySource` read a whole runtime family from the manifest ("a run split out later stays covered without editing a list here", comment at 344–346), and `sourceFiles(join(root,"packages","cli","src"))` at 477 walks a whole directory.
2. **`packages/cli/src` is outside `rawTemplateScopes`.** `browser-test-runner.ts:54–269` is the only multi-line `String.raw` runtime body left in the four packages, and no gate refuses it. There is no `packages/cli/runtime/`; `web`, `node`, `core`, `compiler`, `desktop` all have one.
3. **`check:module-map` (D115 §二) is not implemented** — no `scripts/check-module-map.mjs`, no `docs/contributing/module-map.md`.
4. **The composition-root constraint is TS2341, not style.** D114 line 512: `private` members are unreadable outside the class, proven under this repo's tsc 7.0.2; the only alternative to keeping host constructors in the root is widening the public surface. `VelarWebAnalyzer` (43 private fields, 94 private methods), `WebJavaScriptEmitter` (14 private fields, 22 private methods), `VelarNodeAnalyzer` (11 private fields, 14 private methods) each sit in that position.
5. **The `types.ts` → `types/` re-export precedent** ("`types.ts` re-exports all of it, so every existing `from "./types.ts"` import is unchanged") is the mechanism that keeps `look.ts`'s 55 exports and `project-semantic.ts`'s 27 exports valid across a split; D115 §四 states it as a rule: "门面不变：`analyzer.ts` 等原模块保留并再导出搬走的名字，导入路径全部不变".
6. **Zero-diff acceptance** (D115 §一.7 / §四): the 828-file output fingerprint byte-identical, `protected` seam signatures and public export rosters unchanged, five gates green — measured from the same worktree at the same absolute path `/private/tmp/velar-d114/r0-baseline`.
7. **The existing `packages/web/src/analysis/` collaborators are pure functions**, not host-interface collaborators — only `public-config.ts` declares a host (`PublicConfigTypeHost`). Adopting the compiler's shape means introducing named `…Host` interfaces per collaborator, satisfied by one live-reading object built in the root (the architecture doc's "state half must stay live accessors" note about the emitter applies verbatim to `WebJavaScriptEmitter`'s `cssOutput` / `webOutput` / `needsFileTypeHelper` / `needsLookArithmeticRuntime`).