import {app as routes, describeApi} from "./01-server.vel"
import {findArticle, openArticleStore} from "./03-database.vel"

export const app = routes
export const databaseExample = {findArticle, openArticleStore}

describeApi(app)
